[project]
name = "hatch-inspect-modal"
version = "0.1.0"
description = "Modal orchestration/execution plane for Hatch Inspect"
requires-python = ">=3.11"
dependencies = [
  "modal>=0.73.0",
  "fastapi>=0.115.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
