Parallax's next benchmark is decided: SWE-bench Verified under the Evolving Intent method, chosen over BIRD-SQL after a fast evidence pass on saturation, verifier sealing, upstream fidelity, and existing assets. Frontier models now cluster at 95–97% on [SWE-bench Verified](https://vals.ai/benchmarks/swebench), but the Parallax estimand is a paired matched-vs-evolved contrast at a boundary operating point, whereas BIRD's nominally larger headroom (80% vs a 92.96% human baseline) is substantially annotation noise that would corrupt a sealed verifier. The groundwork includes a concrete slice plan — pinned dataset ingestion from the paper's 50 published Verified IDs, arm mapping with terminal restoration, the official test harness as the sealed verifier, per-source pairing, and cost estimates — plus non-destructive verification that the django-11099 [HUD environment spike](../architecture/episode-spine/swebench-environment/) still works end-to-end (no-op episode test passed in 29 s). Eight enumerated changes generalize the spike to a 10–20 instance first experiment, the largest being re-basing per-instance images on Docker Hub's official `swebench/sweb.eval` images, which were confirmed pullable.

- Main risk: an indistinguishable contrast if models sit at ceiling or floor, as the earlier calibration smoke showed; mitigated by boundary-model screening and a per-instance difficulty spread.
- The static-arm budget confound from the calibration smoke (12 steps total vs 12 per turn) must be fixed before any real run.
