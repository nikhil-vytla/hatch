# Independent gateway accounting review notes

- Review selected source `8743d7282176e2bdbcff68ddc497c8803b55b6c0` and report head `ed8862509fe2cdec98c4856656e845f153e61b44` for draft PR #72. Verify the initial dependency failure and corrected no-product-change condition separately. No provider calls or product edits.

- Verified all nine selected file hashes against Git source and the exact corrected archive. The report checkout is the requested head. The archive SHA-256 and all five corrected log hashes match their retained records.
- Located the earlier private archive by its pinned archive hash. All earlier log hashes and both verifier hashes match. The original condition built the app but failed the test command because Zod was unavailable: 29 pass / 1 fail / 934 assertions. The source correction changes only the CI workflow; report-head additions are evidence/verifier files, not product changes.
- Queried PR #72 and the gateway workflow execution. The report head completed the gateway job successfully, including both frozen installs, build and focused tests. The execution log contains 31 pass / 0 fail / 948 assertions. Checkout executed merge commit `edd5f8db53ff7443fde0a1c83603e5ee43acdbb4`; GitHub contents for all nine selected files at that merge match the selected-source manifest. Its parents are the pinned native-adapter base and report head.
- Read the native validator, gateway, accounting helper, Score implementation and focused tests. Choice/Noul/Score structure is preserved; Score retains a fractional expected index and checks a feasible probability/score rounding interval. The vertex-enumeration oracle is independent of the greedy implementation, covers two through ten levels, and is supplemented by generated rounding cases. No Score-bound defect was found in this bounded review.
- Independently reran the archived focused suite: 31 pass / 0 fail / 948 assertions. No installation, build, provider call or product edit was needed; the already verified archive dependencies were used. The existing loopback redirect fixture remained local.
- Added six pinned-source tests with authored transport responses. Four fail as intended and two controls pass: final onAttempt cancellation resolves success; final onAccounting cancellation resolves success; a pre-dispatch accounting-observer exception leaves a phantom pending attempt; incoherent partial token counts are accepted. Exact fixture observations are in observations.json.
- The observer-exception issue occurs before fetch is invoked (zero calls, one accounting attempt, unknown cost). It is separate from the existing final-observer exception tests, which correctly retain a real paid attempt and avoid retries.
- The partial-usage issue is a validation gap, not a request to infer a missing token field. A known component cannot exceed a known total. Complete invalid usage currently becomes unknown by deliberate policy; this review does not request changing that broader policy.
- Preserved the original four-failure/two-control condition before any correction. Product source remains untouched. Root acknowledged the three findings and will provide separately pinned fixes for follow-up review.
