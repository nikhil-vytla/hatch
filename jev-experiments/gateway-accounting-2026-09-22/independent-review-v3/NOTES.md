# Independent correction review notes

- Review corrected source `d93be44fde84ac83992eaa1c7ca2b1f8359aa99d` as a separate condition. Preserve independent-review-v2 and its four failures/two controls unchanged. No product edits or provider calls.

- All ten selected source hashes match the exact Git source and corrected archive. The archive digest, all five retained check-log digests and the verifier digest match `verification-v3.json`.
- The maintained `observer-usage.test.ts` content from `const payload =` onward is byte-identical to the prior six review tests; only the source-loading/import header changes. Assertion-body SHA-256: `9f43a309891371290e8043b71b834c1ad19d086e93002c5bdc3f81350e2ee11c`. Every v2 review artifact still matches its frozen manifest.
- Product corrections are narrow: cancellation is rechecked after final callbacks; pending callbacks that throw or abort remove only the current undispatched attempt; usage coherence rejects either known component above a known total. Native validator/Score/compose bytes are unchanged from the reviewed v2 source.
- Independently reran the seven explicit maintained test targets: 38 pass / 0 fail / 981 assertions. Added three boundary groups spanning 18 authored cases: 3 pass / 0 fail / 164 assertions. No installation, provider request or product edit was performed by this review.
- Final onAttempt/onAccounting cancellation retains the current observed model, generation id, status, charge, usage and cancellation issue, including when an earlier attempt is paid or its cost/usage is unknown. Unknown earlier observations still keep request totals unknown.
- Pending observer failure/cancellation before the first request yields zero dispatches/attempts and known zero cost. After an earlier paid or unknown 503, it preserves exactly that earlier attempt and its known/unknown accounting, with no phantom retry.
- Partial usage cases cover both input and output above total, explicit zero, a valid lower known component, both components without total, and total without components. Impossible usage is flagged/unknown without losing the independent charge; valid partial observations are preserved without inferring absent fields.
- CI names seven explicit `.test.ts` files, including the maintained observer-usage regressions; this matches the verifier command and avoids recursively running the preserved before-fix review fixtures. Archived install/build/test/publication checks are successful. Remote PR state observed during this review remains the older v2 report head, so its green check is not evidence of corrected-head execution.
- No remaining consequential gap was found within this correction scope. Acceptance of these source changes does not establish remote execution, provider semantics/quality, production billing or deployment. Product files and v2 review evidence remain untouched.
