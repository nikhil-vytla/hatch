# Independent delivery assembly review notes

- Root requested a read-only review of delivery slice selection and the application patch.
- Check complete slice five against current imports/downloads, the 20 selected ignored images, nine retained small checkpoints, and exclusions for caches, private logs, fetched repositories and model packages.
- Do not stage, commit, run models/browsers, or change delivery/product source. Report concrete omissions first.
- `application.patch` exactly matched the live tracked diff plus untracked `api/route.ts`: 96,121 bytes, 23 files, SHA-256 58663d91310786d13c375119324d3ab74bf0eaccc9ba98b548f08780ce55ce74. Replaying it against Git base bytes in a disposable directory reproduced every current target byte.
- Ran the real assembler for runtime, routing, training, playable and integration without `--stage`. The latest snapshot had 556 selected files, including 20 selected WebPs, 14 final-review PNGs and nine authored checkpoint files. All nine checkpoint hashes/byte counts match the manifest; all selected binaries are below the artifact size limit.
- The first static scan flagged ignored crowd/demo.json and ghost-brush/examples.json. Source inspection confirmed both are deliberately generated by tracked live-worlds/prepare.ts from tracked JSONL/manifest evidence before the app build. Recorded these generated dependencies explicitly; no product or delivery edit was needed.
- The scanner also sees retained harness fixture imports and strings that generate tests, plus two directory URLs. Inspected them as non-application references. The final pass checked 342 resolved module/URL edges, 25 glob matches and the Mac installer's complete authored-source list.
- No forbidden cache/virtual-environment/dependency/repository tree, private `.local.` file, `.log`, archive, model package or extra weight file was selected. High-confidence credential patterns found no matches in selected text. Public harness transcripts are intentional evidence.
- Initial guessed filenames for an old clean-checkout shell script and per-scene builders did not exist; actual code uses clean-checkout.py and the shared live-worlds/prepare.ts. This was a navigation correction, not a delivery omission.
- Reported passing selection/patch results to root before writing the final report. The temporary repository was removed, its index stayed empty, and no source, stage or commit was changed. The verifier omits its own overwritten JSON file from its per-file inventory to avoid a recursive hash snapshot.
