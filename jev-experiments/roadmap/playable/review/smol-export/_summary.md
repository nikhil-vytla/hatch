Smol's two completed Core ML comparison streams each retain 2,960 decisions, with full split coverage and 100% argmax agreement against the saved MLX predictions. Independent arithmetic reproduced maximum probability differences of 5.69e-6 on CPU and 4.08e-6 with automatic hardware selection, and all source, prediction, checkpoint and package hashes matched. Public rows retain semantic IDs and probability comparisons while excluding passages, prompts and gold distributions; the UI distinguishes prepared-input Core ML timing from complete native MLX timing. The [verification script](verify.py) and [recorded checks](verification.json) use no model, GPU, browser or performance runs.
