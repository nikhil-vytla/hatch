# Independent Smol Core ML artifact review notes

- Root requested an offline verification of the completed Smol Core ML streams while Qwen and Laya continue running.
- Recompute row counts, split coverage, probability deltas and argmax agreement; compare the saved report and immutable provenance. Inspect retained fields for dataset text and public timing/coverage labels.
- No model calls, GPU work, browser automation, or changes to training/root files. New review artifacts only.
- Read the completed Smol export, public row schema, publication, exporter/row-publication source and the current study UI. Initial guessed filenames for row publication/UI were stale; located the actual `export_decision_rows.py` and `TypedDecisionStudy.tsx` with `rg --files`. A full corpus-manifest print was too large; subsequent inspection used parsed summaries only.
- Added an independent standard-library verifier rather than calling the production gate or publication script. It reads only Smol cache/package bytes and frozen source manifests; it does not load tensors or model libraries. The training owner confirmed CPU file reads were acceptable during the active queue.
- The first semantic-ID character check was too strict for BANKING77's original `reverted_card_payment?` category. Manual inspection found a question mark in that identifier, not leaked free text. Relaxed that review check to include `?`; all option strings remain short semantic IDs matched against their source identity.
- Final verifier passes: 2,960 rows per compute condition; 192 validation, 384 held-out test and 2,384 transfer. Every split has 100% coverage and argmax agreement. Overall maximum deltas match exactly: CPU_ONLY 5.692243576049805e-6; ALL 4.082918167114258e-6.
- All 400 Typed Decisions cases retain five questions each. Original row/split/option identity, probability arrays, public per-row flags and report aggregates match exactly; no duplicate IDs or malformed distributions found.
- Verified both public/raw Core ML stream hashes, nine original MLX prediction hashes, three retained/cached readouts, eight feature-cache files, eleven pinned backbone files, all three package files and fixed prepared timing tensors. Protocol/corpus/source/model manifest identities match. No model package or source text was copied into this folder.
- Public rows contain only IDs, split/status, semantic values and numerical comparisons. The public Smol report equals the raw export, links both streams, and its UI explains prepared 768-token Core ML inputs versus complete variable-length MLX timing. Timing source checks were read-only; no fresh latency/performance measurement was run.
- Verification limitations: package description bytes were hash-checked, not parsed again as protobuf; saved hashes cannot establish execution timestamps or hardware placement. No Qwen/Laya artifact, root/product/training file, stage or commit changed.
