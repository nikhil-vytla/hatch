Jev's default-package review caught a fresh-install failure caused by checking for model files too early and an integrity gap that omitted the MLX helper and other shipped sources. The owner added dependency-only installation diagnostics, tests with the proposed registry present from the start, and a complete shipped-source manifest. [CPU-only fixtures](probes.py) confirm that the new installation diagnostic succeeds without model files and that changing the helper blocks promotion. Production defaults remain null; actual package installation and inference remain separate gated work in the [Mac toolkit](../../../mac/README.md).
