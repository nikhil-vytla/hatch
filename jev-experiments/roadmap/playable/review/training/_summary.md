Jev's saved typed-decision results reproduced exactly across all 27 model, seed and evaluation-split combinations, with the full 400-case Typed Decisions transfer set retained. Temporary fixtures exposed stale and empty evidence accepted by the completion checker and publisher; independent rechecks confirm those cases are rejected. The unsupported-score edge case and calibration definition were corrected, and future Core ML reports explicitly describe their timing boundary. The [CPU-only probes](probes.py) record the checks at this checkpoint; later export and robustness completion belongs to the [study release status](../../../training/release-status.json).
