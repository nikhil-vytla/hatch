# Training and evidence independent audit notes

- Bounded read-only audit requested by root: consequential metric, selection and provenance defects, at most five concrete findings.
- Coordinate with training/runtime owner, who is actively fixing Core ML NaNs. No inference, model loading, downloads, GPU or training. Use small CPU fixtures and existing evidence only.
- Read the frozen protocol, corpus transformations, training/metric code, robustness runner, complete Core ML exporter, release checker and compact publisher.
- Current source/corpus manifests match. The cache contains 768 adaptation, 192 validation, 384 held-out and 2,384 transfer decisions, including all 400 Typed Decisions cases and 2,000 questions. No malformed gold distribution or exact task overlap between splits was found.
- Recomputed all 27 saved-prediction model/seed/split aggregates without inference. Every aggregate matched exactly, every prediction row matched the frozen source row, and selected recipes/temperatures matched their recorded validation minima.
- Temporary malformed evidence made the real release checker and publisher claim researchEvidenceComplete=true. The fixture used stale corpus hash, wrong backbone revision, wrong export model/seed, empty comparisons, empty baseline and fake robustness seeds. The current real release remains blocked; this probe does not imply current measured predictions are fabricated.
- Sent four bounded findings to the training owner: completion/provenance gate, zero macro NLL for no supported rows, ECE's hard argmax-agreement interpretation for soft labels, and Core ML tensor-only timing versus MLX complete-decision timing. Owner agreed to strengthen validation/provenance and add validation-only selection checks.
- Tool friction: one broad manifest read produced excessive output. Switched to aggregate JSON scripts and targeted source ranges; no corpus text copied into the report.
- First independent rerun rejected the original malformed/stale gate fixture, returned null for unsupported-only macro NLL, preserved all27 exact real metric matches and passed8 study/selection unit tests. Publication now defines argmax-agreement ECE; future Core ML reports label prepared-tensor timing.
- Reviewed the new selection CLI. It now runs training/export evidence validators before using its pure ranking helper; a separate fixture confirms wrong-model exports with otherwise qualifying validation numbers remain ineligible.
- One final T1 regression remained in the partial fix: publishing alone trusted an old release-status completion flag after export/robustness evidence changed. Retained `probe-results-partial-fix.json` and asked owner to derive the audit inline. No GPU work was needed.
- Final rerun confirms publishing now derives status inline and changes the stale complete flag to false. Invalid training is rejected, wrong-identity selection remains null, unsupported-only NLL stays null, and all27 actual metric groups still reproduce exactly. Eight study/selection tests and ten Mac tests pass. Fresh per-kind robustness, baseline provenance and export evidence remain the training owner's pending runs; this audit does not mark them complete.
