# Live matched-queue games

All six unassisted Jev games completed across seeds 7, 19 and 42. The button condition cleared 0/0/0 lines; reachable-landing decisions cleared 0/3/1. Both conditions topped out on every seed. These three pairs change both information and action authority, so they do not establish a wording effect or broad playing ability.

The [frozen protocol](PROTOCOL.md), [summary](summary.json) and [complete requests and replies](games.jsonl) retain availability failures, stale decisions and cancellations. Every lane recorded zero local-demo, fallback and human controller time. The 700 ms grace value remains preserved, but assistance was disabled to isolate model control. Provider failures affected seeds 19 and 42; reported successful cost was zero, while failed/cancelled request costs remain unknown.

Run `bun jev-experiments/roadmap/tetris/record.ts` with a caller-owned `AI_GATEWAY_API_KEY` to record a fresh cohort after preserving or moving existing outputs. The recorder refuses to overwrite its original results. Full playable games remain uncapped, and the existing suite verifies handoff, branching, stale responses and the adjustable 700 ms default.
