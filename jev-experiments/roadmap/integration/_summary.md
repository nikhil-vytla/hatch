Three real coding-client sessions used Jev's MCP delegation tool to propose a bug fix, add tests and analyze an authored fixture. [OpenCode](https://opencode.ai/docs/mcp-servers/), Claude Code and [Codex](https://learn.chatgpt.com/docs/extend/mcp?surface=cli) applied changes through their own tools and passed independent tests. The evidence retains provider rejection, approval defaults, a working-directory failure and malformed-output recovery instead of presenting configuration parsing as integration success. Separate real-client runs covered interruption, malformed output, unsupported task requests and unavailable routes, with no late or rejected artifact applied. Client examples, a short skill and explicit evidence gates document the bounded workflow.
