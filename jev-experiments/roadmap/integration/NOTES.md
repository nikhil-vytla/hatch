
2026-09-20: Created isolated, authored TypeScript sum fixtures for the three installed coding clients. Each starts with a failing inclusive-boundary test. Actual MCP discovery/call/result audit is kept separately from host transcripts so local fixes cannot be confused with delegated fixes.

The first Claude host model failed provider retention policy before calling the router. The first Codex session had default MCP approval rejection before server execution. The first OpenCode session used the parent project's effective cwd and timed out; the corrected command explicitly uses --dir. All failures are retained. Corrected OpenCode, Claude and Codex runs independently passed10,5,6 tests respectively after receiving actual bug/test/analysis artifacts.

OpenCode recovered from one malformed repository-analysis result with another bounded request. Codex recovered from missing explicit context without removing the required-shell restriction. All three observed no eligible shell route and unsupported typed image questions. Active end-to-end cancellation per client remains outside the demonstrated fixture result; stdio cancellation has a provider-free test.

The fixtures do not demonstrate routing superiority, savings, production readiness, or universal client compatibility. The short skill and per-client configuration examples were shaped by observed failures. Original generated fixture content from OpenCode's wrong-cwd attempt is kept under its failure evidence, not in the live app.

Active interruption suite completed for all three actual clients against a delayed loopback destination. It waits for the tools/call audit event before sending SIGINT. OpenCode disconnect produced an explicit cancelled server result. Claude reported aborted_tools; Codex exited while its tool call remained in_progress. No delayed answer was applied. No cancellation notification is inferred where the audit did not record one. Added stdin-EOF abort handling to the MCP server so client disconnect cannot leave its pending delegate running.

2026-09-21: Malformed destination suite completed for all three real clients. Each made one actual route_task call to a deterministic loopback response, received error, wrote ANALYSIS.md and left tracked source/tests unchanged. The original failing test remains deliberately failing. Added evidence_index.py to derive per-condition gates, usable artifact counts, host identity, client versions and chronological audit order. Failure variants are explicitly historical and failed in-file; corrected runs require three usable artifacts and independent tests. The reproducer now refuses overwriting existing evidence. Scoped future OpenCode read/edit permissions to the generated absolute fixture path with external_directory denied; original successful recordings predate that refinement.

2026-09-21: Added FAILURE-PROTOCOL.md before boundary-only sessions in all three real clients. Existing workflow audits already include no-route(required shell) and unsupported typed-question results. The additional session isolates actual route_task contract rejection (empty id, otherwise complete prompt/source) and a valid task with an empty registry. The intended gate requires exactly two calls, complete source/test context, readable unsupported/unavailable explanations, zero destination attempts and unchanged source/config. It does not interpret a rejected contract input as a model-capability result. Canonicalized generated temporary fixture paths before constructing scoped OpenCode permissions, so macOS /var symlink spelling cannot differ between cwd and allowed paths.

Boundary suite results: Claude and Codex passed on the first condition; OpenCode's first absolute-path scope allowed both MCP calls and preserved source/config, but denied the required ANALYSIS.md write. Kept it failed. Official read/edit source confirms relative worktree path matching. A separately named corrected OpenCode condition with relative file allowlist passed. Each successful boundary session made exactly2calls with complete context (OpenCode368bytes, Claude367, Codex382), returned unsupported then unavailable, had empty attempt arrays and left source/config unchanged. No delegated model or GPU ran. Updated index:16sessions,12conditionpasses,4retainedfailures. Manual review confirmed all three explanations distinguish invalid id from empty registry; incidental host timing speculation is not endorsed as performance evidence.
