# Mac toolkit notes

- 2026-09-20: Started a local-only toolkit implementation. Model files must be installed with verified checksums; supplied `.eml` files are read without modifying mailboxes. No cloud fallback is permitted.
- Fresh install at `/tmp/jev-local-fresh-test` created a new Python 3.13 environment and installed only inference dependencies. The installer copied the existing repository's original MLX equations, not fetched upstream source code.
- A pinned 842 MB Laya weight file, encoder configuration and tokenizer were installed from the local cache with exact size and SHA-256 verification. The inference path loads only local files.
- Five unit checks pass for typed/byte limits, MIME decoding, unchanged input files, bad-checksum rejection and missing-model failure without network access.
- The frozen 12-message authored email smoke set produced 7 correct labels, with all messages covered. The complete run blocked socket connections and checked original file hashes. This weak result is published in `email-evaluation.json`; the runtime remains experimental, with no automatically selected model.
- Independent review found and reproduced four defects across the toolkit/study: predictable temporary-path symlink writes, incomplete CLI error envelopes, tiny ordinal levels collapsing through decimal rounding, and stale feature arrays being relabeled current. Fixed all four; the reviewer independently reran provider-free probes and confirmed preservation of the unrelated file, complete error fields, exact tiny ordinal identities and stale-cache rejection without manifest rewriting. Installation now also verifies the exact marker content.
- Added an explicitly selected `laya-readout-experimental` model using the new predefined seed17 readout. A fresh toolkit installation downloaded the complete pinned backbone from the remote URLs into an empty directory, verified its hashes and installed the bundled 12 KB readout.
- The installed readout runtime matched 15 saved validation decisions spanning all three kinds with maximum probability difference 1.05e-7, while socket connections were blocked. The same 12-message email set still scored 7/12. Both model choices therefore remain experimental and no default is selected.

- Tightened JSON file reads to the declared byte cap before loading the model. CLI totalMs now includes file preparation and model initialization; in-process Runtime.decide remains a warm-call measurement. Ten provider-free tests pass, including both boundaries. Registry defaults are supported but remain unset until validation/export/package gates pass.

- A second independent review caught a selected-default installation failure: the final installer diagnostic tried to load absent model files before model installation. Added doctor --runtime-only and made the prospective package test install the selected registry from the start. The review also prompted a shipped-source manifest covering the original MLX helper and all executed/copied package inputs; stale source bytes now prevent promotion. Independent CPU probes and eleven Mac tests pass, with actual promotion still gated on complete exports and fresh package evidence.

- Added installed CREDITS.md with Laya authorship, pinned revision and Apache-2.0 attribution, upstream source, the repository's original MLX helper provenance, MLX's MIT license, and the distinction between the new residual readout and the earlier workflow specialist. Its exact bytes are included in both the shipped-source manifest and installed-copy verification before default promotion.

- After all training/export evidence passed, validation selected the predefined Laya seed17 readout. Installed the proposed default registry into a fresh toolkit and rehashed the previously remote-downloaded model into an empty model directory. Installed credits and runtime/helper/checkpoint/requirements bytes matched the source manifest. Diagnostics passed; invoking decide without --model returned both fruit answers correctly while socket connections were blocked. Only then promoted laya-readout-experimental as the default. Runtime/installer/credits/registry bytes are frozen for final routing integration; subsequent edits are report text only.
