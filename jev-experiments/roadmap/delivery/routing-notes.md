Drafted routing-pr.md from the repository PR template and the PR-description skill. Counts were checked against routing/README.md, routing/fresh-install.json, integration/README.md and routing/comparison/README.md. The body distinguishes current local checks from historical model/client evidence and leaves branch-specific checks and PR links pending.

Scope assumes ModelRoutingLab.tsx and evidence-links.ts belong to the later app PR because they depend on app components and Vite assets. No PR was created or pushed. No model calls, evidence rewrites or implementation changes were made during this documentation follow-up.

The final execution-identity review updated the body to 61 routing tests and 364 assertions, after exact model matching guards and a refreshed installer smoke. Earlier 56-test counts describe the preceding source snapshot. No provider samples were repeated.

The isolated runtime+routing verification now passes against a clean archived Git base, including two frozen Bun installs, 78 combined tests/414 assertions, TypeScript and the installer smoke. The routing PR must include the shared mac/models.json dependency. Later app files remain excluded; branch CI is still for root delivery.

Final registry promotion rerun: routing tests still pass 61/364 and roadmap tsc passes. fresh_install.py and verify-isolated.py --base origin/main both pass and produce the same 83,696-byte bundle with SHA-256 af53f146ee76f199254ac9b866cae70b02b5e525ae4b218a7e280a17fcab68a9. The isolated check records base eb3c18d6db2891f0dfeff87d6a760d5936be37f4, 78 combined tests/414 assertions, two frozen Bun installs and no provider/GPU calls. README and PR wording now distinguish the Mac toolkit's validation-selected experimental default from the router bridge's explicit model configuration; historical explicit-model MCP evidence remains unchanged.

Final source-review correction supersedes the preceding local check counts: 66 routing tests/398 assertions pass, with an 84,933-byte fresh bundle, SHA-256 fadcab2dac09c7ee03368bd3f7df7316c9195f8920e5d297731c199165d56a9f. MinimumQuality remains an aggregate measured hard floor; escalation requires improvement in both aggregate and task-calibrated measured quality. The parser accepts unambiguous SQL/Lua comment deletions and rejects incomplete per-file headers. The frozen count-normalization protocol and historical model/client artifacts were not changed.

The final isolated check passes 83 combined tests/448 assertions, frozen installs, TypeScript and fresh installation against origin/main eb3c18d6db2891f0dfeff87d6a760d5936be37f4. The repository-root installer smoke matches the isolated bundle hash above. Bun includes cwd-relative source-path comments, so invocation from the roadmap folder produces a different checksum without different executable statements. No installer source changed.
