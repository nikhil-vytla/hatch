[
  {"id":"cal-loop","split":"calibration","prompt":"Fix the inclusive sum bug. Return kind structured, with text a JSON string containing operator, the corrected loop comparison operator only.","context":"function sum(n) { let s=0; for(let i=1; i<n; i++) s+=i; return s; }\nContract: sum integers 1 through n inclusive for nonnegative integer n."},
  {"id":"cal-tests","split":"calibration","prompt":"Write numeric test cases that expose the inclusive sum bug. Return kind structured, with text a JSON array of 2 to 4 objects with n and expected. Use integer n between 0 and 20.","context":"Correct result for sum(n) is the sum of integers from 1 through n. Buggy code: let s=0; for(let i=1;i<n;i++)s+=i; return s;"},
  {"id":"cal-dag","split":"calibration","prompt":"Analyze the repository dependency graph. Return kind structured, with text a JSON object with cycle, a boolean for whether any cycle exists, and reachable, the sorted module names reachable from a including a.","context":"Directed dependency edges: a -> b, b -> c. Modules: a, b, c, d. Module d has no edges."},
  {"id":"cal-overlap","split":"calibration","prompt":"Fix a half-open interval overlap bug by calculating correct overlap lengths for the supplied cases. Return kind structured, with text a JSON array of four numeric lengths in case order.","context":"Intervals [start,end) use length max(0, min(endA,endB)-max(startA,startB)). Cases: [[0,3],[2,5]], [[0,2],[2,4]], [[4,6],[0,2]], [[1,9],[3,6]]. Buggy implementation counts shared endpoints as length one."},
  {"id":"test-search","split":"heldout","prompt":"Fix the binary search bug. Return kind structured, with text a JSON object with replacement containing the corrected assignment for the marked line.","context":"Find first index at which sorted a[i] >= target. let lo=0,hi=a.length; while(lo<hi){const mid=Math.floor((lo+hi)/2); if(a[mid]<target){lo=mid;}else{hi=mid;}} return lo;\nMarked line: lo=mid; May stall when hi=lo+1."},
  {"id":"test-median","split":"heldout","prompt":"Write cases that expose an incorrect even-length median implementation. Return kind structured, with text a JSON array of 2 to 4 objects with values and expected. Each values array must be sorted, nonempty, contain 2 to 6 integers, and have an even length.","context":"Correct median for an even-length sorted array is the average of the two central values. Buggy code returns values[values.length/2], the upper central value."},
  {"id":"test-cycle","split":"heldout","prompt":"Analyze this repository graph. Return kind structured, with text a JSON object with cycle, whether any directed cycle exists, and reachable, sorted module names reachable from a including a.","context":"Directed edges: a -> b, b -> c, c -> a, c -> d, x -> y. Modules: a,b,c,d,x,y."},
  {"id":"test-concurrency","split":"heldout","prompt":"Fix a scheduling counter's endpoint bug by computing the correct peak simultaneous jobs. Return kind structured, with text a JSON object with maxConcurrent, a number.","context":"Jobs use half-open [start,end) times. Jobs: [0,3), [1,3), [3,5), [3,4), [4,6). At a shared endpoint, jobs ending there stop before jobs starting there begin. The buggy counter processes starts before ends."}
]
