Model Routing Lab now exposes one TypeScript selector through a Bun CLI, local MCP server and web preview. Hard restrictions and callback metadata are checked before usable outcomes; bounded delegates return artifacts for the host to review and apply. Actual coding-client fixtures and an eight-task routing comparison preserve failures, unknown costs and explicit replay limits. The design borrows task-boundary routing and separated outcome records from [auto-model-router](https://github.com/fstandhartinger/auto-model-router) and inspectable simulations from [Whichmodel](https://whichmodel.app.mintapis.com/). The public inspector downloads protocols and retained client evidence, while 61 provider-free checks and a fresh installer smoke cover the final SDK boundaries.
