
2026-09-20: Created routing and integration work folders. Inspected the existing routing experience and the three installed harness CLIs. Current versions are Bun-installed OpenCode 1.18.31, Claude Code 2.1.278 and Codex CLI 0.154.0. Reused the root's shared decision contract rather than creating another validator.

Read auto-model-router upstream README, Whichmodel playground HTML, and official OpenCode, Claude Code and Codex MCP documentation. Whichmodel failed via the web reader twice but loaded via curl. The parent-provided Fable config path needed a jev-experiments prefix. The papercuts skill CLI refused logging because this repo has not opted in; no log was created.

The toolkit uses no additional dependencies. The selector rejects hard-budget execution with unknown or simulated prices and uses full cold token bounds for reservations. Runtime output is a typed artifact; the router never applies a patch. The OpenCode destination denies all tools and receives only supplied context. Protocol v1 was written before integration outcome collection.

Independent review caught four runtime issues: redirects could escape loopback, cancellation during verification could return success, custom classifiers ran before cap/locality checks, and malformed MCP null input caused an unhandled rejection. All now have guards and provider-free probes. Fable protocol review findings 11–18 also led to full route validation, measured-only minimum-quality checks, failed-classifier identity preservation, OpenCode identity disclosure, a conservative system-envelope allowance and explicit notes for skipped verification. Classifier category/difficulty now changes soft scores only when the registry has corresponding evidence. Hard eligibility is independent.

Actual OpenCode delegation initially used steps:1, which produced an unsupported assistant-prefill request for Fable. Raising the delegate step bound to4 resolved it. OpenCode host integration needed an explicit --dir because its effective project root differed from subprocess cwd. The timed-out attempt and its generated fixture are retained. Claude Fable host access failed under its provider retention policy; Claude Sonnet 4.6 host succeeded while delegates remained configured Fable 5.1 Global. Codex required a documented per-tool approval configuration for the automated fixture; its default-denied attempts are retained. These findings informed the configuration examples and short skill.

All three corrected harness runs actually invoked the server, delegated bug/test/analysis work, applied changes and passed independent Bun tests. OpenCode needed a second analysis attempt after a malformed result; that recovery is visible. No end-to-end cancellation behavior is claimed from completed-call cancellation notifications. Provider-free stdio tests verify active cancellation.

The first web BYOK call returned malformed JSON-shaped output. A changed protocol adds strict JSON schema and concrete artifact examples. The next model response had incorrect unified-diff counts; the root preserved it and used host git apply --recount, with disclosure, to obtain a passing independent test. Artifact parsing now validates hunk arithmetic and preserves malformed raw output for inspection instead of silently treating it as usable.

The bounded routing study froze tasks and graders before destination inference. Root host judgments were recorded before viewing any destination answers or graders. Author category/difficulty labels were written before inspecting local-model outputs, although the local invocation had started; this ordering is recorded rather than implied otherwise. The study is eight synthetic fixtures with four held out, uses aggregate flat quality calibration, and therefore cannot establish classifier-driven routing improvements.

The completed first comparison could not compare accessible alternatives because Haiku returned errors; a separate32-token diagnosis showed the account's free-tier model restriction. The protocol was amended before new benchmark calls to probe GPT4.1 then GPT4.1 nano on a neutral ready request and choose the first API/schema-eligible route. An overstrict exact-ready sentinel in the probe was corrected to the declared valid-artifact gate, selecting GPT4.1's existing ready-with-punctuation response. The unnecessary nano probe and original null-selection file remain. No task was rerolled. GPT4.1 passed4/4 calibration and3/4 heldout; mini passed3/4 calibration and2/4 heldout. All routing classifier conditions still selected mini under the frozen weights. This is a useful negative policy result, not a superiority or savings claim.

Built and installed a 54 KB Bun CLI/MCP bundle into /tmp/jev-router-fresh-test. doctor, stdio initialize/list and the explicit synthetic receipt.eml example passed. The email file remained unchanged. The installer does not alter shell/client config and includes a prefix-only uninstall instruction. Real model inference remains in the separate Mac runtime.

2026-09-21: Prototype review found duplicated web/CLI HTTP execution with diverging usage validation. Extracted http-executor.ts; shared finite/nonnegative usage checks, cached-token cap, strict response schema, redirect rejection, cancellation and raw malformed output retention. Null/primitive response bodies now return malformed, not unavailable. OpenCode always reports cost null. Optional MCP audit errors no longer terminate discovery or execution. Regression test initially wrote via Bun stdin.end(data), which ignores data; changed to write then end. No production failure was hidden by that test correction.

Built explicit localRuntime bridge from the existing operations to the installed Mac subprocess. Provider-free contract tests validate arguments, stdin, identity checks, temporary email cleanup and missing-executable errors without fallback. With a GPU slot released by the training worker, actual MCP decide and classify_eml calls passed for both the frozen base and experimental adapted readout. Their records are smoke evidence only and select no default. GPU was released immediately afterward.

Fresh current source-built CLI install passed doctor, three-operation stdio discovery, explicit lexical receipt classification, unchanged email bytes and uninstall. The bundle is approximately65KB. Evidence records the bundle checksum and exact size. Prototype review dispositions and historical harness variants are explicit; no new remote destination sample was rerolled here.

The root's strict live rerun again returned a semantically useful five-line patch with a six-line new-count header. Rather than repeat the unchanged model request, added the versioned single-hunk normalization condition. It changes only decimal count fields when one complete LF text-file hunk is otherwise structurally valid, preserves the original malformed proposal and explicit repair metadata, and shows disclosure in the web result. Independent tests reject the original counts, strictly apply the normalized proposal, run the task tests, and reject it against mismatched source. Forty provider-free tests pass. Top-level authored routing TS/TSX files were formatted with Prettier3.9.8; immutable comparison tasks/graders and recorded JSONL were not rewritten.

Final guard: validate SDK policy before any custom classifier, including finite budget checks. Regression proves NaN maxCost never calls the paid classifier. Forty-one tests and123 assertions pass. The root's one new live request under the normalization condition retained the bad original count, exposed repair metadata, then passed strict host git application and five task assertions. No earlier failed condition was replaced. Final fresh installation is rebuilt after this guard; use fresh-install.json for the exact bundle bytes/checksum.


2026-09-21 final SDK review: Reproduced verifier locality/metadata failures with provider-free hooks. Before the correction, six of seven new probes failed; null executor output also escaped as a TypeError. Added validated verifier identity, explicit skipped/error/cancelled records, readable evidence retention, finite accounting and cancellation handling. Only valid adequacy false can trigger escalation. A rejected first artifact is no longer returned if the stronger destination fails. Unknown and overflowed total charges remain null. Custom callback implementations remain trusted caller code, not network sandboxing.

The root also identified classifier identity consistency: a declared-local callback returning hosted/remote metadata did invoke the destination in the first reproduction. Source/execution consistency checks now stop that task and retain reported identity separately from the declaration. Null or malformed classification cannot corrupt the response shape. The direct selectRoute remainingBudget probe widened a zero cap before the fix, and [null,validRoute] crashed duplicate-id detection; both now pass with finite remaining-budget validation, cap clamping and safe invalid-entry handling.

Added direct Vite ?url&no-inline evidence downloads rather than embedding JSONL in JavaScript. The lab has optional protocol/comparison and three client groups with summaries, audits, complete retained host transcripts, applied diffs and independent test output. Evidence remains historical and unchanged, and the four-held-out-task offline policy replay has no savings claim. The root owns browser/download verification. All 56 routing tests/338 assertions and roadmap tsc pass. A fresh temporary-prefix installation passed doctor, three-tool discovery, receipt classification without modification and uninstall; bundle 81,971 bytes, SHA-256 066ef314d0f9cbee978ad7a12e5db5310c9b4f4745a2c0762da9a939b240cc0e. No GPU or remote sample was repeated for this review.


Final execution-identity review: Three provider-free regressions initially reproduced OpenCode destination.model disagreeing with route.model yet reaching the executor, an HTTP response naming another model while returning success with the selected model's price, and a custom executor substitution accepted as success. The selector and direct OpenCode entry point now reject configuration contradictions before execution. Shared response checking rejects explicit substitutions while retaining reported identity, valid usage and raw content, with charge null and no usable artifact, fallback or verification. Positive test fixtures now report their selected model; the substitution condition has its own regression. Missing HTTP identity and all configured-only OpenCode outcomes remain explicitly unverified.

Added a direct-executor subprocess test with an empty executable PATH so a guard regression cannot invoke OpenCode or a provider. No actual provider or GPU call was repeated. All 61 routing tests/364 assertions and roadmap tsc passed. The rebuilt installer passed doctor, tool discovery, unchanged input and uninstall; bundle 83,629 bytes, SHA-256 c5e843b0626b1169412dad5e7c104b904a605bd805472d975242ba5fae864f51. Updated delivery PR counts and the downloadable protocol after these corrections; the earlier browser-download record refers to the preceding protocol bytes.


Final artifact audit inspected 225 existing text files/1,260,889 bytes and parsed 633 JSON documents/JSONL rows. Credential-pattern, structured-field and exact 11-environment-secret scans found no matches. All 64 Markdown links, 48 evidence-index references and 21 Vite assets resolve. Home paths are benign worktree/plugin provenance; the historical cwd failure also lists five unrelated tracked filenames but did not read their contents. Measured artifacts were left unchanged. Reworded earlier final-test paragraphs as historical stages, with 61/364 current.

Built verify-isolated.py and ran it against archived Git base d2c8ff30e9fa45008191ffd89273f87fdcc07f9b. The copy contains runtime+routing source, the runtime's existing-code patch and mac/models.json only. Fresh locked dependencies, 78 combined tests/414 assertions, TypeScript and installer checks passed. The bundle matches 83,629 bytes and c5e843b0626b1169412dad5e7c104b904a605bd805472d975242ba5fae864f51. No provider, GPU, git branch or commit was involved. The script records source hashes and keeps its generated test output separate from historical research evidence.


Final Fable source-review findings 5/6: Reproduced escalation to a higher-aggregate but lower-task-score destination, rejection of valid SQL comment deletion, and failure to normalize otherwise unambiguous SQL/Lua wrong-count patches. Four focused probes failed before correction. Kept the standing minimumQuality aggregate measured floor and documented why classifier predictions cannot change hard eligibility. Shared qualityForTask now supplies ranking and escalation, with escalation requiring both aggregate and task-calibrated measured improvement. Both conflicting-score directions and a valid stronger route are covered.

Header recognition now distinguishes ordinary deleted `-- ` comments from structural a/b headers. The root's additional diff --git/incomplete-second-header example also failed before the small per-file state guard. Correct and missing old/new header variants, with and without Git metadata, now pass their regression expectations. SQL and Lua tests require strict git apply failure on original wrong counts, then strict check/application and exact resulting source after count-only normalization. Ambiguous path-shaped body lines remain refused by the normalizer. HUNK-NORMALIZATION.md is unchanged at SHA-256 2055cdd2ae1f1573750179137ef4167a0d2dc1cb3ab663454ab7b2a8d7f5285c; single-hunk-counts-v1 retains its original transformation and metadata semantics. Historical provider/client records remain untouched.

All 66 routing tests/398 assertions and roadmap tsc pass. Fresh installation passed diagnostics, three-tool MCP discovery, unchanged-file lexical classification and uninstall. Final bundle: 84,933 bytes, SHA-256 fadcab2dac09c7ee03368bd3f7df7316c9195f8920e5d297731c199165d56a9f. README and delivery body now describe these corrections and distinguish current checks from earlier evidence. No providers, GPU, browser or performance calls were made.

Clean isolated runtime/router validation against origin/main eb3c18d6db2891f0dfeff87d6a760d5936be37f4 passed 83 combined tests/448 assertions, both frozen dependency installs, TypeScript and fresh installation. A diagnostic explained the initial 408-byte bundle difference: Bun's source-path comments vary with invocation cwd. Reran the documented fresh-install command from the repository root; its 84,933-byte bundle matches the isolated check exactly. The installer was not changed.
