# Local typed-decision study notes

- 2026-09-20: Started the bounded training/runtime implementation. Existing `local-models-and-games/apple` code and recorded results will be inspected before extending the study. Earlier workflow-specialist evidence remains a separate condition.
- Existing cached weights and Python environment are available on an Apple M4 Max with 48 GiB RAM. Existing Core ML package is a workflow specialist trained on Typed Decisions and cannot serve as the new general-model adaptation result.
- Chose an explicitly bounded frozen-backbone residual-readout study. The protocol fixes 256 train / 64 validation examples per adaptation dataset, 128 held-out examples, all 400 Typed Decisions transfer cases, three seeds and validation-only selection. Full inference exports remain mandatory; readout-only exports will not count.
- Pinned seven upstream sources and generated immutable corpus manifests: 768 adaptation examples, 192 validation examples, 384 held-out examples, and 2,384 transfer decisions. SummEval supplies source text, 11 references, 16 system summaries and mean fluency values per document. All 400 Typed Decisions cases contribute their 2,000 questions.
- State-blind and lexical baselines ran on the frozen corpus. Their held-out macro NLL values are 1.54022 and 1.40012 respectively. These are candidate/ordinal derivatives, not original benchmark scores.
- Initial Laya feature extraction was stopped while a standalone email smoke invocation briefly overlapped it. Those overlapping timings are discarded; all subsequent GPU measurement is serialized.
- Feature extraction exposed an implementation error: Laya's final scorer has 1,024 input dimensions, not 256. Corrected dimensional inference from the actual weight shape and narrowed unsupported-input handling so tensor-shape errors fail the run instead of being reported as coverage rejections. Discarded the failed extraction files before retrying.
- Fable's pre-fit review prompted deterministic exact-task duplicate checks, summary-only SummEval fluency inputs, explicit unseen-label prior rules, fixed candidate text forms, case-preserving choice targets, exposure disclosures and token-ID/parameter-count requirements for complete Core ML exports. The amended corpus has zero selected cross-split exact-task exclusions. No readout had been fit before this amendment.
- The pinned Laya source checkout at `42626c348753fbb17572a813127df2278a1ec527` marks BoolQ as in its training mix. Exact pinned-weight train/test exposure is not established. The provenance table distinguishes that known task exposure from unsupported claims of test leakage.
- Added complete-graph conversion code for all three backbones, including dequantizing the pinned Qwen MLX weights for the PyTorch conversion bridge. Every graph includes token embeddings, transformer backbone, residual readout, masking and calibrated probabilities; a failed export is written as a failed result.
- Laya extraction completed with full coverage of the frozen corpus, including all 2,000 Typed Decisions questions. Native MLX residual-readout training selected learning rate 0.0001 and epoch 3 using validation macro NLL, then repeated seeds 17/29/43 with temperature 1.3 selected separately on validation. Held-out derivative macro NLL was 0.92836 / 0.92667 / 0.92724; argmax agreement was 60.94% / 60.94% / 61.20%. These numbers do not establish general-purpose ability or resolve upstream benchmark exposure.
- Paused GPU work after Laya training to permit the routing integration's bounded local-classifier batch. The shared host must serialize these runs before reliable timing evidence can be recorded.
- Independently checked the MTEB SummEval mirror against the official 1,600-row human-annotation file. All 1,600 summary strings and fluency values match a mean of the official expert fluency annotations. The verification script and official-file SHA-256 are retained; the downloaded annotation text stays in the ignored cache.
- Corrected accuracy scoring to accept any member of a tied soft-gold argmax set. A first-position tie break would make the score depend on arbitrary option order. This does not change NLL, recipe selection, temperatures or any model prediction; aggregate accuracy reports were regenerated from the same saved predictions.
- Independent review prompted stricter feature-cache provenance and per-file hashes, actual backbone-file checksum checks, and separate validation-only Core ML agreement fields. Final test/transfer parity can fail the release gate but cannot choose the installed default. No default has been selected.
- All three candidates completed the frozen three-seed recipe. Qwen selected learning rate 0.0001 and epoch 1; its held-out macro NLL spans 1.1775–1.2070. Frozen-readout and mean-input-embedding baselines plus full adapted option-reversal/batch/timing checks are complete. Causal readouts are strongly order-sensitive, so useful average metrics do not imply stable decisions.
- First full Laya conversion attempt verified the complete PyTorch bridge against one validation decision of each kind, with maximum probability error below 1.5e-7. Core ML conversion then hit the known NumPy 2.5 scalar-cast incompatibility in coremltools 9. Added the same explicit one-element scalar conversion used by the earlier repository exporter; this changes conversion plumbing, not model math.
- Float16 complete exports returned NaNs on the first validation decision. Finite attention masks and precomputed rotary tables did not resolve the error; failed attempts are retained separately. The float32 export returns finite distributions and is being evaluated across all validation, held-out and transfer decisions under CPU_ONLY and ALL. Precision choice uses validation behavior only.
- Independent CPU review rederived all 27 trained seed/split aggregate reports exactly. It found permissive release checks, a zero NLL placeholder for entirely unsupported inputs, an ambiguous ECE label and unequal timing boundaries. Tightened evidence/publication checks, use null for unavailable NLL, documented the unchanged hard-correctness ECE definition, and labeled Core ML prepared-tensor timing separately from MLX end-to-end timing.
- Added a provider-free validation-only default selector. It ranks the predefined seed17 exports; repeated seeds remain uncertainty evidence, with no best-seed choice. A separate package promotion is required. No model has been promoted while exports are incomplete.

- All three complete float32 graphs passed CPU_ONLY and ALL comparisons on every supported validation, held-out and transfer decision. Laya agreed on 99.966% of 2,960 decisions per condition; Smol and Qwen agreed on 100%. Maximum probability deltas were 2.30e-5 or smaller for Laya, 5.70e-6 for Smol and 3.09e-5 for Qwen. All three had 100% validation argmax agreement.
- The first Laya run predated retained prediction streams. Repeated the exact hash-verified package without refitting or reconversion, retained both complete streams, and remeasured fresh-process Core ML timing. Six authored public JSONL files now preserve 17,760 decision comparisons with source text and gold omitted. An independent offline review verified every Smol row and its full chain of artifact/input hashes.
- Refreshed provenance-linked baselines and complete adapted batch checks across all three kinds and seeds. All strict training/export/baseline/robustness gates pass. Publicly released test labels are used where present; the disposition clarifies an overbroad protocol sentence without changing the frozen protocol, corpus or scores.
- Validation-only selection chose the predefined Laya seed17 readout at macro NLL 0.8843, versus Qwen 1.0972 and Smol 1.5150. The actual proposed-default installer passed in fresh toolkit/model directories, verified installed sources and credits, and returned both authored fruit answers correctly while networking was blocked. Promoted laya-readout-experimental only after this package proof. All candidates remain experimental; the earlier workflow specialist and weak 7/12 email result remain separate evidence.

- Corrected a final publication-only status inconsistency: completed, audited training now publishes status complete independently of export state. Original fitting result files retain their historical trained_readout_only_export_pending status. Regeneration changed only the three published training status values; fitting/export evidence, selection, registry and shipped package sources stayed byte-identical, and the release audit remains ready.
