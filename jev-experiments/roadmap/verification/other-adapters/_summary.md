Both Jev adapter suites passed in temporary macOS arm64 workspaces using Go 1.23.12 and Rust 1.98.1. The [reproduction script](../other-adapters.py) obtains official toolchains, verifies the Go archive checksum, isolates dependency/build caches and removes them afterward. Source and checked user-global configuration hashes remained unchanged, and no adapter edits were required. The [result JSON](../other-adapters.json) retains the commands, versions and successful test output.
