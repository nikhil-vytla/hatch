# RewardBench 2

- User selected RewardBench 2 to replace IFEval in experiment 28.
- Implement Jev as a ranker of the benchmark's published answers, scored against upstream chosen/rejected labels, with separate subset reporting and explicit provenance.
- Retain the earlier content-audit findings and add content notices where needed; no Qwen generation or Gemini reference is needed for supplied candidates.
- Pinned Ai2's dataset at `7ff08853b0d5686e79b13fda8677024f566a104a`, and scoring code at `05a9005efb607249822c193590c8ecab87c77052`. The dataset contains 1,865 cases and 8,977 supplied candidate answers in six subsets.
- Upstream IDs repeat across subsets. An early cache guard caught the collision before reusing a mismatched record. Case keys now include both subset and ID.
- The initial integration pilot scored all answers within a case against a shared state. Gateway headers reported a 30-request/minute and 250,000-token/minute limit, with minute-long Retry-After waits. It would take too many calls to finish the full split this way.
- Re-read TypeSafe's primitives documentation. Each question is independent, and question instructions may carry structured data. Switched to a consistent independent-answer protocol: shared evaluation policy in state, one prompt and one candidate inside each question, batched under 90 KB and 128 questions. This also matches scalar reward-model evaluation more closely. Discarded all pilot scores from the published run; the pilot cache remains ignored.
- The full independent run packs 8,977 questions into 255 initial batches. Requests respect Retry-After; individual candidate scores and protocol/input hashes are checkpointed for resumption. Missing responses are retried, not relabeled as poor judgments.
- An explicitly requested GPT-5.6 Sol content review flagged 41 non-Safety cases. All 450 Safety cases receive a reveal gate. A narrower review identified three prompts and six candidate answers across four cases to omit from public payloads because they contain explicit sexual descriptions involving minors. Original inputs remain only in ignored local caches; scoring includes all cases.
- Added a hash-checked, repeatable publication transform so both committed JSONL and generated public JSON omit those exact fields. Source text changes cause a preparation error. This preserves positions, labels, scores, and hashes.
- Scoring tests cover top-score tie credit, unavailable requests, paired Ties margins, the 1% upstream bonus, zero-spread edge cases, label/model leakage, independent batched inputs, and partial-score resumption. Publication tests cover repeated preparation and changed-source refusal.
- Browser integration rejected this session's authentication. Used the Playwright CLI skill instead, with screenshots and interaction checks. One test initially matched the generic word "prompt" in the evaluation policy; narrowed it to the actual fixture prompts.
- Gateway diagnostics confirmed that larger batches exhaust the account's observed 250,000-token/minute quota while request quota remains. Added steady batch pacing and recorded safe quota/usage metadata. These are recording-run controls, not shared limits on the visitor-key playground. Provider responses identify the model as the `typesafe-ai/jev` alias.
- A separate live question-isolation smoke check hit the same quota and will be retried after the benchmark. The structural isolation tests pass; the live check is not claimed as complete.
- Reviewed the supplied `models` column. Some values identify human edits, source methods, or anomalous metadata, so the app calls this field "upstream attribution" and preserves it without inventing a generator model.
- The original scorer download now has its own SHA-256 guard before its three scoring functions are extracted for parity validation. The fetched file stays ignored; it is not copied into the commit.
- Browser assertions passed for blind answer selection, per-case reveal reset, ranking-mistake filtering, empty search results, Safety content hidden before reveal, the Ties margin explanation, and no horizontal overflow at 320 pixels. Light and dark layouts were inspected through screenshots. Existing server/credential tests passed 17/17; new benchmark/publication tests passed 10/10.
- Added a per-case result explanation so visitors do not have to infer success by comparing four cards. It identifies the highest-ranked and preferred answers, split credit for ties, and the valid-versus-invalid margin in the Ties subset. Ranking uses full-precision scores even though cards display two decimal places.
- Full run finished: 1,865 cases, 8,977 candidates, 255 completed batches, 80.9002% category mean. Three earlier 429-only failed batches were recovered. No cases remain unavailable. All recorded successful batches report zero gateway cost.
- The first live exact-equality check failed: 8.88 alone versus 8.86 batched on an authored France/Paris fixture. Four follow-up repetitions per condition both ranged from 8.87 to 8.88, with identical means of 8.8775. Preserve this result and show the limitation in the app; do not turn it into a passing determinism claim or infer a general batching effect from one fixture.
- Python's `splitlines()` initially split Unicode line separators inside JSON string values. Changed the parity reader to split on literal LF, which is the JSONL record delimiter. The TypeScript reader already used LF.
- The full source-integrity validator passed, including all nine public omissions. The original pinned Python functions and TypeScript implementation matched exactly for every category, including all 51 Ties pairs.
- The final local build passed. An exact-value scan of all 48 public build files found no embedded gateway key. It also verified full case coverage and 41 non-Safety notices in prepared JSON.

## Final visual check

The inherited sticky positioning made the three explanation cards overlap while scrolling on desktop. RewardBench now uses a spaced, static stack for those cards. A first combined patch failed on a stale notes heading and applied no changes; the corrected patch used exact source context. A notes append initially used a repository-relative path from the app directory and was retried from the repository root. The archived IFEval summary now explicitly says RewardBench 2 superseded it.

The final build passed after the scroll fix. An exact-value gateway-key scan and token-signature scan found no secrets in the 50 staged files or 48 built public files. The default Bun Vercel CLI cache had a missing UUID dependency; retrying with an explicitly pinned CLI version uses a separate installation. A credential-helper search included a nonexistent legacy path; the existing helper was located under experience-prototypes/scripts.

## Publication

The pinned Bun Vercel CLI worked. Created PR #55 under nikhil-vytla, then deployed commit 97093d30deb824c818dbd32536b4c605358a00e8 directly from GitHub to the existing Vercel project. The build reached READY and assigned jev-experiments.vercel.app. Public HTTPS verification confirms an exact SHA-256 match for the benchmark JSON, all 1,865 cases and 8,977 answers, 41 case notices, nine omitted fields, the repeatability report, and HTTP 404 for the retired language JSON. No repository merge was performed.

Production browser checks passed for the displayed completed score, a blind Math-category pick and reveal, resetting the pick on the next case, dark mode, and no horizontal overflow at 320 pixels. Final follow-up changes contain only research documentation and verification evidence; the deployed app code is unchanged.
