# Jev tools and RL experiment notes

## 2026-09-22: Jimothy source review

- Started a bounded primary-source review of Andrew Prifer's Jimothy repository and public demo. This is roadmap research; no inference, installation or product implementation is planned in this pass.
- The text browsing tool could not retrieve either primary URL. Continuing through read-only HTTP source inspection; any fetched files stay outside this authored research folder.
- The existing GEPA domain-adaptation decision extends prompt optimization. Check overlap before proposing another experiment.

## 2026-09-22: Jimothy source review completed

- Pinned Andrew Prifer’s MIT-licensed repository at `f2ad9b40b88ea913d38fda758e564eac5f12fc0b` (default branch `master`), package 0.1.2. Read training, schema, metrics, calibration, runtime, model provenance and browser/email documentation. No upstream source, data or weights were added here.
- Verified that the retrieved live demo script exactly matches the pinned source. Inspected HTTP content only; no model loading, inference, teacher requests or installation was performed.
- The default encoder stays frozen; the fitted linear head serves one fixed task. Upstream email results measure teacher agreement, omit one class in the test support and show material singleton/batch differences.
- Proposed a browser email specialist with independent human references, explicit abstention and runtime comparisons. Preserve the completed typed-decision study and installed default. Root owns integration and checklist decisions.

## 2026-09-22: Workflow Builder source review

- Reviewed Chris Nicholas’s Jev Workflow Builder at `9a652d22216028a64ccaa7468754e45e1860618b`. The package declares Apache-2.0, but the pinned tree has no standalone LICENSE and GitHub does not detect one; keep those facts distinct.
- Read graph types, editor, execution, provider adapters, REST runs, room storage, example authentication and run previews. No installation, hosted room creation or model calls were made. Upstream source remained outside this authored folder.
- Verified bounded DAG execution with typed branches, OR/AND joins, named outputs, run traces and labeled mocks. Identified integration requirements: graph-revisioned replay, complete cancellation, explicit malformed/unsupported states, persistence completeness, and real permissions/budgets before hosted collaboration.
- Proposed a next-wave trace/branch experiment within the existing React Flow and routing experience, with a frozen authored task suite and independent task grading. No root map or application files changed.

## AutoJev source review

- Reviewed pinned code, public model metadata, license/NOTICE, playground and historical result provenance. No weights, inference or training were run. Exact curated training data is absent; selected validation results do not establish an independent final-test result.
- The proposed text decision adapter preserves actual model identity, full distributions, supported limits and calibration coverage. It remains separate from the Mac default, Jimothy specialist and GEPA prompt search.

## webctl source review

- Reviewed Kyle Wild's pinned MIT-licensed Go CLI, retrieval/filtering code, provider MCP client and benchmark methodology. It is a web-search and relevance-filtering tool already using Jev, not a browser controller.
- Upstream token comparisons exclude some selector/summarizer costs and cannot expose a native harness payload. The new protocol requires complete accounting and independent answer/evidence checks. Malformed successful chunk responses need explicit coverage tests.

## Recruiter and inference-path review

- Reviewed the pinned recruiter, Browser Use attribution, official DeepSeek API/model encoding and SGLang score schema/handler. No real-person data, model installation or inference was used.
- V4.1's supported non-thinking encoding already emits the closing think delimiter at the assistant boundary. Score requests need fully encoded prompts and label-token IDs; the exact preview-server combination still needs parity tests before performance measurements.

## RL rubric-grader design

- Read primary GRPO, rubric reward, judge reliability, reward-overoptimization and judge-injection sources. Checked existing judgment-reliability, RewardBench, reward learner, GEPA and routing accounting in this repository.
- Existing judging artifacts are reusable evidence infrastructure; the earlier full-information linear reward optimizer is not GRPO. Proposed frozen rollout replay measures reward ordering and advantage error before any training pilot.
- Grading work scales with updates × prompts × rollouts × semantic criteria. Speed and cost savings remain hypotheses; no exponential claim is supported by that multiplication.

## Integration

- Renamed the authored folder to cover all six additions after the user extended the research batch.
- Kept named decisions separate from implementation checklists. New application/model work remains next wave; the image-search implementation has its own folder and patch.

- Independent review checked the RL protocol arithmetic and split rules, plus pinned SGLang score handling and V4.1 encoding. It found no consequential mismatch; exact deployment compatibility and performance remain unmeasured.
- Reuse review: all six are research tickets, so no new runtime abstraction was extracted. Proposed implementations reuse typed decisions, immutable records, existing React Flow and routing/tool boundaries; per-experiment metrics and state remain specific.
