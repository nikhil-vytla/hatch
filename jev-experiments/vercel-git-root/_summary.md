[Jev experiments](https://jev-experiments.vercel.app) now uses `jev-experiments/experience-prototypes` as its Vercel root, matching the application's package and API configuration. GitHub `main` is the production branch, Bun installs the locked dependencies, and outside-root files remain available for evidence preparation. The first clean Git build revealed a broad results exclusion in `.vercelignore`; the correction retains committed JSONL inputs while excluding private fixtures. Project settings and deployment evidence are recorded alongside the report.
