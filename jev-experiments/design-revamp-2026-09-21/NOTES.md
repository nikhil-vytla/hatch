# Design work notes

- The user wants taste, thoughtful design engineering and experiment writing with code, observations and learnings. A coherent UI/UX pass belongs before the first public release. Backward compatibility is not a design constraint.
- Revisit the earlier six-builder study and add primary references that inform specific decisions. Separate what was observed, what Jev should borrow, and what remains an untested proposal.
- Deliver `jev-experiments/DESIGN.md` as the durable philosophy and decision record. Keep implementation status explicit.
- Extended the existing six-builder study with Ciechanowski, Victor, Case, Webb, Krehel and Patel. Four sites received page-only visual captures; the other two are primary text/source research. The board preserves those distinctions and links every creator.
- Wrote DESIGN.md around an instrument notebook: a quiet shell, subject-specific scenes, clear execution labels and authored notes. Recorded the sitewide revamp as a first-release requirement, with implementation still open.
- Built an original editorial study using the existing materials engine. Two matched starting scenes differ only in the custom contact rule. The note connects the result to a real code excerpt and explains the limits of the grid simulation and random-number pairing. No new dependencies.
- The first visual pass had too much unrelated material in the terrarium preset. Replaced it with a focused pool-and-powder starting scene, and moved time controls above the figures so the first action remains visible.
- Independent review caught an inaccurate `water → wood` caption; the powder transforms, so the label now says `powder → wood`. Added a mobile enlargement control after finding the side-by-side contact boundary too small to inspect.
- Fault injection found that unavailable canvas drawing left an indefinite loading state. Added a visible failure message and retry action; the note and source remain readable. Repeated the fault injection and observed no page error.
- Browser checks covered 1440 × 1080 and 390 × 844, keyboard scrubbing, replay/pause/reset, scene download, code copy, explanation disclosure, dark mode and reduced motion. Exported scenes pass the existing scene parser. JavaScript-disabled reading remains available. These are spot checks, not phone-hardware or screen-reader certification.
- The first file lookup assumed the engine was nested under playable; the file inventory showed roadmap/materials. The repository has no papercut log opted in, so no log was created. Browser CLI run-code results must be returned to appear in command output; console.log alone did not provide the requested measurements.
- Fable 5.1 reviewed an explicit design-only snapshot through OpenCode/AWS Bedrock; the session export confirmed the requested model. Its source/screenshot critique informed clearer transport labels, persistent rule labels, CSS dark fallback, quieter announcements, readable metadata and a higher starting powder column. Recorded every disposition, including retained side-by-side defaults and deferred platform/performance checks.
- Verified the explanation against actual exported states: the branches begin with identical cells and seed, first wood appears at step 13, and each branch still has 1,188 water cells at step 96. The final powder/wood counts are 138/0 and 114/24. Rebuilt from a clean main-based worktree without dependency installation and checked the roadmap patch applies there.
