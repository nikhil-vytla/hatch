# 0006: Record operational corrections and reused work

Status: Accepted

Sonnet requests returned 403 for this account. Gemini 2.5 Flash-Lite completed an access check and became the writer default. Its five-request-per-minute allowance requires shared pacing. No account upgrade or alternate region was used. A separate reference pass reused the fixed local Qwen candidates and Jev choices; retries completed only missing writer answers. Earlier failures remain in local logs and the published run history.

The installed GEPA version calls its reflection function with a string, while the initial adapter expected messages. The corrected run accepts either representation and reuses completed random, hill-climb, and OPRO validation searches. It does not alter their prompts based on test outcomes. The initial broken GEPA result is superseded, not evidence about GEPA's quality.

Reactive MiniGrid policies repeatedly visit exactly the same state. Cache successful decisions by the complete ordered request, including recent history for the memory policy. Every replay step records whether it used the cache. Do not cache failures, infer hidden state, or describe repeated episodes as independent fresh model samples. This compares deployable cached policies across seeds.

Resume completed episodes and reconstruct their cache from successful recorded wire responses when an interrupted run is continued. Four workers schedule independent episodes without a barrier between seeds. Identical in-flight decisions share one request. These scheduling changes preserve the policy inputs and all 240 planned episodes.

The papercut CLI found that this repository has not opted in. No root log was created. Setup friction remains described in NOTES.md.
