Structured instructions and criteria now survive the [Jev language adapters](../adapters/) without being flattened into text. The shared [native question definitions](../packages/decision-runtime/src/native.ts) distinguish Choice, Noul and ordered Score, reject invalid annotations and preserve answer metadata. An exact Git archive passes the app build, nine Bun tests, 59 Python tests, Go and Rust checks, and all 45 publication records. One optional Python test is skipped; provider quality, training and release deployment remain separate gates.
