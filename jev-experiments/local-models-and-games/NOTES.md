# Local models, games, and experiment organization

- Created this investigation folder before research. Scope: native MLX decision inference and fine-tuning, a Core ML export, shared-prefix open-model baselines, Snake and an original 3D game, benchmark research, new experiment ideas, and clearer names/categories.
- Hardware is Apple M4 Max with 48 GiB unified memory. MLX sees the GPU. Installed MLX, Core ML Tools 9.0, and MLX LM in the existing ignored Python environment. Core ML Tools warns that installed PyTorch 2.8 is newer than its tested 2.7; export and actual predictions must be verified.
- Inspected pinned Laya source 42626c348753fbb17572a813127df2278a1ec527 and model revision 1c5edc17a7acd8701df6fc341c0d179f1c62c982. Source and downloaded weights stay in ignored caches. The original MLX port matches PyTorch logits within 0.000018 across six questions, including mixed padded batches.
- Typed Decisions revision ea9306458d6e9563628369a3d1e72e362fb381d2 supplies 1,200 training cases and 400 test cases. Each has five questions. Targets are averages of a synthetic teacher's distributions, not human ground truth. Fine-tuned specialist and untuned generalist results must be labeled separately. Packing keeps every instruction, criterion, and state token; the longest training question is 634 tokens, within the explicit 768-token limit.
- User steered toward Eric Zhang's SGLang approach and multiple models. Preserve the working Laya baseline, add a native MLX shared-prefix and one-token decision implementation across model sizes/families, and benchmark on matched cases. This reproduces the serving strategy, not SGLang's CUDA runtime or its particular 35B model performance.
- TypeSafe's Doom demonstration uses structured state, not screenshots. The new arcade exposes engine-computed immediate action previews and probabilities; it does not imply Jev invented the physics or shortest-path features. Recording six seeded Jev episodes alongside a greedy code baseline. Game-clock playback is distinct from API wall-clock time.
- Three deterministic engine tests pass, including vacating-tail Snake behavior and requiring return to base after drone pickup. Early file searches used two nonexistent singular/legacy paths and were corrected using the file inventory. An MLX docs URL was unavailable, so official source examples were used. Searching for the exact name sgent did not find a credible blog match; asked the user for its link while continuing independent work.
- Applied the domain-modeling skill. Existing categories mix chronology, verbs, and subjects. The planned subjects are Productivity, Creative tools, Games, Agents & tooling, Benchmarks, and Training & local. Titles should name the actual task or named benchmark; descriptions can retain personality.
- Corrected source received: https://sgnt.ai/p/jev/ resolves. Read the post and followed its primary links. It distinguishes first-token label scoring, full-option likelihood, and a trained scorer. Its claims about Jev's architecture are explicitly speculation; do not present them as verified facts. TheoLeeCJ/openjev now redirects to SemIf, which already has an MLX path. daseinlabs/open-jev also runs MLX and scores full option strings; add a small controlled method comparison rather than claiming our MLX port is the first.
- Jev Typed Decisions recording paused after 144 cases when all six gateway retries were exhausted. The recorder resumes from per-case files; no failed API call is a scored decision. Restarted the remaining cases.
- Native MLX fine-tuning completed in 883.9 seconds on M4 Max. Held-out teacher agreement rose from 36.35% to 46.95%; KL fell from 0.57382 to 0.35531. This is a useful negative/partial result, not a production-ready clipboard model. All 400 Jev comparison cases subsequently completed after resuming failed requests.
- The first Qwen cache parity check failed at 0.25 raw-logit difference with the checkpoint's default low-precision activations. Changed floating activations/scales to float32 while preserving packed 4-bit weights. Cache versus full-prompt differences fell below 0.00007 for the first model. Disclose this compute choice alongside latency.
- Qwen 4B separates its chat template into a .jinja file. Initial download allowlist omitted it, so loading the chat template correctly failed. Added .jinja and reused the pinned manifest revisions. No fallback template was invented.
- Build and the existing credential-isolation/gateway/record tests plus new deterministic game tests passed: 20 tests, 252 assertions. An initial manifest-edit command used the wrong working directory; corrected the path and rebuilt.
- Completed all three causal-model label readouts and the full-option Qwen 0.6B comparison, 2,000 decisions each. Qwen 4B label scoring reached 54.05% teacher agreement, Qwen 0.6B labels 35.35%, full-option Qwen 0.6B 33.60%, SmolLM2 labels 25.45%. Full-option scoring reduced overconfidence compared with single-token Qwen 0.6B but did not improve its agreement. Jev reached 72.75%.
- Cache checks passed across all four workflows and reversed question orders. Maximum raw-label difference was 0.000203; every reversed-order probability difference was exactly zero. Full-option cached/fresh mean-log-likelihood differences were below 0.000025.
- Coremltools 9 lacked aten::new_ones, then hit NumPy 2.5's removal of implicit one-element array-to-int conversion. Added local conversion handlers that preserve shape and dtype, including scalar ones. The float16 Core ML package now executes on CPU_ONLY and ALL. All 12 initial argmax checks passed; maximum probability difference 0.01709. Warm fixed-shape latency was 165.6 ms CPU-only and 92.0 ms with ALL; no claim about actual Neural Engine placement. Running the complete Core ML test set to quantify any precision-induced decision changes.
- Browser checks: Snake replay loads, manual button and focused-arrow-key controls advance the expected state, and keyless live play asks for a caller key without making a model request. Research map tabs and category filtering work. Removed the old layout-study switcher from the four new published views, and compacted their headings. One Playwright reference became stale after hot reload; refreshed the snapshot before continuing.
- Content triage found no matches for the selected profanity, explicit sexual/self-harm, or violence/identity-abuse patterns in the 400 new cases. Read representative complete states from all four workflows. Security examples discuss operational incidents; no claim of exhaustive offensive-content detection.
- Train/validation/test manifests preserve original IDs and state hashes. No exact state overlaps were found between the full training partition and test.
- Full Core ML evaluation exposed 60/2,000 argmax changes despite nearly identical overall teacher agreement, 46.90% versus MLX 46.95%. Mean maximum probability difference was 0.006945; the worst case was 0.14228. Precomputing rotary tables in float32 yielded identical predictions on the full test, so it did not explain the drift. The export remains an explicitly approximate float16 artifact; the app reports the full-test disagreement, not only the passing 12-case spot check.
- The training-label prior reached 48.95%, above the tuned head's 46.95%. Added a prominent interpretation note: the training workflow works, but this specialist is not ready for automatic private form filling. It needs stronger adaptation and an independent form benchmark.
- Final build and 23 Bun tests passed, including full evidence alignment, replay integrity, data split isolation, credential isolation, and gateway retries. Three analytic Python metric tests passed. Exact configured-key scan found no recording credential in the changed source or complete production dist. Desktop and 390px layouts have no document-width overflow; workflow/model filters retain 100 cases for the selected workflow.
- Browser QA caught undefined initial SVG motion attributes when mounting Snake. Added explicit initial circle geometry and verified the reload. The 3D arena uses a closer camera for legibility; keyboard and manual controls, keyless live messaging, and research filters were checked.
