# Quality and simulation review

## 2026-09-20

- Started a systematic review requested by the user after PRs #55 and #56 merged.
- Scope: one independent subagent audit per catalog experiment; inspect setup, evidence, simulation quality, failure modes, benchmark completeness, and useful library integrations.
- Review source, published data, and UI behavior. Separate measured findings from proposed changes and avoid treating model/provider failures as interchangeable.
- User examples to investigate deeply: upward-biased music motifs, Café Jev with menu exploration and customization, and full benchmark runs instead of sampled comparisons.
- Fetched origin/main, verified no source diff from the previously completed branch, and created jev-quality-simulation-review at merged commit 4c0c40d.
- Inventory contains 33 catalog entries. Two new review agents started; a third spawn hit the service thread limit. Reassigned the existing idle critique agent to JudgeBench to use available capacity. Dedicated reviews will proceed in waves.
- Papercut CLI refused logging because the repository has not opted in with PAPERCUTS.jsonl; no opt-in file was created.
- Early music evidence: five completed motifs all ascend or stay level before the final reset, and current playback ignores duration and instrument choices. Beverage evidence: four substring checks match every completed authored example, and unsatisfiable menu combinations are absent. These are agent findings pending report integration and spot verification.
- Completed browser route sweeps at 1280 px and 390 px across all 33 routes. Waited for lazy-loaded arenas and the recorded generative UI before measurement. No page exceptions, visible alerts, or document horizontal overflow in these initial-state checks. This does not establish interaction correctness or audio quality.
- JudgeBench audit reproduced the published 77% per-orientation score and official 63% two-order score from the same 100 pairs. Full upstream release has 620 pairs. Drink and music probes preserve the concrete counterexamples.
- Created an interactive local review artifact and strict completeness validator. The partial build is for review development only; final build requires Markdown and structured JSON for every catalog ID.
- Wrote six cross-experiment proposals grounded in official library docs, with separate model decisions, deterministic execution, and independent outcome measures.
- Independently reran the music, beverage and JudgeBench probes. Reproduced 31/40 discarded music durations, 23/23 literal beverage baseline matches, and 63/100 official two-order JudgeBench successes.
- Browser reproduced the beverage provenance mismatch: after manually selecting lemonade, the visible request remained warm/no caffeine/no dairy and the inspector retained an older Jev herbal-tea decision. No live request was made.
- The optional scope question has not changed the task; continuing with the full audit and concrete designs before production implementation.

- Teach audit: read publication mapping, current catalog and Learning component, teacher runner, export/parity test. Confirmed real TF-IDF logistic updates and deterministic random/least-confidence acquisition. Published result uses one 160-case pool, 80 authored test rows, and four label budgets. Current React component replays curves and teacher labels; it does not expose labeling or retraining actions. Next checks reconstruct records and measure unique template coverage.
- Six dedicated audits complete: music, beverage, JudgeBench, classification, stability and RewardBench 2. Independently reran classification/stability count probes; source split counts and paired prediction flips reproduce.
- Latency review found 120 sequential logical requests, 12 unique request bodies, 43 requests with retries, and repeated copies of one Noul fact rather than diverse workloads. Agent is preserving exact timing/denominator evidence in its report.
- Verified current Vercel Jev page advertises a free promotion ending 2026-09-25, with 32K context. Added dated source-backed coverage recommendations; no paid evaluation calls have been made during this audit.
- Teach probe: reproduced the two published final accuracies with the checked-in Bun predictor, 43/80 random and 42/80 uncertainty. Generated 160 pool rows contain 140 distinct texts; 80 test rows contain 77 distinct texts. All 51 teacher record labels match the menu oracle, but they cover 47 distinct texts. Swapping held-out warm/chilled leaves every probability unchanged because both words are out of vocabulary. Scoring all 768 authored held-out templates, without new model calls, gives 50.91% random and 44.79% uncertainty. Verified BANKING77 official split counts and active-learning/annotation library documentation online.
- Music browser check: playback starts and exposes Stop, then stops through the UI without console errors. Captured the current dark-mode piano roll. This check confirms controls, not musical listening quality or the static startup-race hypothesis.
- Preserved desktop/mobile sweep JSON and two representative current-app screenshots. The interactive audit reader supports search, category/verdict filtering, source links, and dark mode; 390 px check found no horizontal overflow.
- Teach audit complete: wrote experiments/teach.md and teach.json with five evidence-backed findings, a correction-and-retraining session, a 20-seed authored protocol, and a full BANKING77 protocol. Retained probes/teach.py and teach.results.json. JSON schema fields checked. No app edits, paid calls, model training, package installs, browser automation, or commits.

- Added Tetris/drawing engines, recorders and app components. Five frozen Tetris seeds, seven policies, exact code baselines; drawing uses33case-method combinations with honest geometry references. Recording not yet started while full JudgeBench job runs.
- User added wardrobe spoken-edit/video try-on experiment. Verified FAL credential variable names without values. Delegated implementation using existing agent after platform thread limit prevented new thread.
- Smart paste initial zero-row report was a JSONL skeleton misread, retracted publicly; actual three presets contain15decisions. All review tooling must use decodeRecord.
- Retried publication update after wrong relative working directory; Bun installed @fal-ai/client1.10.1 successfully.

- Development browser checks were interrupted by Vite full reloads when parallel agents regenerated public evidence. Switched root QA to Vite preview on5195 so interactions persist while independent preparation runs. Do not interpret those interrupted attempts as app transaction failures.

- Browser transaction regression passed: preexisting title preserved, later location correction survived undo, switching preset cleared undo history. Earlier UI automation timed out on an exact select label because its wrapping label includes option text; switched to the observed combobox accessible name.
- User requested retrospective and deeper creative-coding research, then explicitly emphasized independent real-time simulation and requested grill-with-docs/show-me prototypes. Read grilling/domain-modeling/show-me skills; design questions pending. Existing experiments and recording continue.
- Wardrobe preparation now copies nested speech assets. Fixed another incorrect root-relative path from app cwd; subsequent multi-folder scripts use an absolute task root.

- Undo audit: decoded the original published record with shared readRecord and verified its bytes plus the complete Undo component against frozen commit 4c0c40d. One request supplies five scores; e1/e3 are the selected color edits. The actual callbacks pass all 32 independent-field subsets and restoration checks, but a selection edit or completed rerun resets an already applied undo. Reproduced old selections applying after request edits and late answers replacing manual selection. The current preview has no design-edit controls or actor metadata. Synthetic repeated-field histories expose why its overwrite formula cannot become a general selective-undo engine. No browser, provider, or app changes; fixed a probe-only console variable typo before the successful rerun.

- Orbital audit complete against frozen main 4c0c40d5. All 192 transitions replay exactly; both controllers win 3/3, Jev matches greedy 95/96, and none of six trajectories offers an unsafe immediate alternative. Independent no-damage route search finds seed 42 needs 26 moves instead of 30. A crafted detour defeats greedy with a 70-move loop despite a verified 10-move solution; no Jev claim is made for this uncalled case. Code-only seeds 0..999 all win with greedy and 26 contain duplicate hazards. Reproduced dock-height semantics, inspected world-clock/handoff/pause behavior in source, and reran the shared engine tests: 3 pass, 125 assertions. Wrote experiments/orbital.md/.json and probes/orbital.ts/.json. No browser, app changes, provider calls or commits.

- Changes audit complete: decoded the sole four-judgment venue-change record and executed actual Changes callbacks offline. All four predictions match a code intersection using the depends arrays sent to Jev, so the stated independent check is not held out. Reproduced a venue response attached to an edited date (travel/reminder mismatch) and synthetic absent answers displayed as Still supported; the actual published record is complete and the gateway checks coverage. Wrote experiments/changes.md/.json, probes/changes.ts/.json and a small changes.audit work folder. Verified React Flow/jsdiff primary docs. No browser, app edits, paid calls or commits.

- App build and all 104 tests passed after icon-studio integration. Dedicated audits reached 21/33; remaining12 assigned individually across the three reused workers. Added ignore rules for generated public assets and a worker-created Vite dependency cache to avoid committing fetched library copies.
- Tool friction: attempted bun run build from repository root, then ran successfully from experience-prototypes. Preview navigation to a hash retained an old JS document; reload loaded the new catalog correctly. Neither was an application defect.

- Original Logos audit: five completed cases and one HTTP 429 failure across six briefs, 20 choices, all five successful structures single. The original component is unchanged from 4c0c40d. Bun callback probe reproduces an old seed-library result under an edited astronomy brief and loss of a manual coral palette; current inspector omits the brief and retains stale model provenance after manual variation. Enumerated all 240 specifications into 60 geometry/weight strings; calculated light orbit stroke of 0.108 px at 16 px size without claiming raster failure. Report and probe stay separate from new Icon studio.

- Local-models audit complete: decoded all 400 cases/20,000 distributions and independently reproduced agreement, KL and Brier within 4.3e-9. Tuned Laya matches all five teacher answers on 10/400 cases, prior on 30/400, Jev on 89/400; tuned-minus-prior paired case bootstrap is -2 points with interval -4.8 to +0.8. Qwen 0.6B label/option paths have 10/20 and 12/20 constant-argmax question groups. Confirmed 60 Core ML argmax changes and a changed-choice MLX margin up to 0.13939. All exact split overlaps are zero. Two applicable integrity tests pass 64,810 assertions. Wrote local-models.md/.json and original probe files. Corrected a probe syntax typo; used direct official pinned-card fetch after the web reader rejected its raw URL. No inference, training, app edits, browser checks or commits.

- Routing audit complete: 20 unique authored tasks, 20 correct after one recovery versus original 19/20 coverage. Read exact local request bodies, recovery history and metrics. A post-inspection lexical diagnostic matches 20/20, explicitly not a held-out result. Actual callbacks reproduce an old calculator response replacing selected classification example 3; live prompt and all seven route descriptions differ from recording. Wrote experiments/routing.md/.json and probes/routing.ts/.json with a reusable actual-component harness. Fixed missing React.Fragment in the probe before successful rerun. No browser, model calls or app changes.

- Adaptive forms audit: the original 81-state table has 45 correct terminal choices and 35/36 optimal split questions; s11xx asks sweetness of two unsweetened drinks. Actual callback rollouts identify all eight existing menu items in three questions but recommend a drink for all eight unsupported complete preference profiles too. Only 15 states are reachable from the default tree; code independently owns terminal decisions. Probe/report separate menu-item identification from hard-preference elicitation and specify full finite evaluation with deterministic cost baselines.

- Benchmark-atlas audit complete: record contains eight benchmark cards and twelve product ideas, nineteen Proposed and one Implemented, with zero structured revision/split/scorer/budget/coverage fields. Verified primary split sizes and scoring boundaries for all eight named benchmarks. MASSIVE needs a version choice: 1.0 has 51 languages/151,674 test utterances; 1.1 has 52/154,648. WildGuard requires accepted access terms; SciFact official test labels are not public. Report specifies a 14,470-case BoolQ/ANLI/PAWS tranche, explicit request volume and a manifest-driven study planner. Wrote benchmark-atlas.md/.json and original count probe. No benchmark/model calls or app changes.

- Verifier audit complete: all 20 published rows are five exact states repeated four times; no verdict flips, but three groups have score drift. Exact local wire responses contain 20 additional evidence judgments discarded by the row builder. The live UI omits that question and changes criteria. Actual callbacks reproduce late verified output replacing a selected failed-import trace and malformed JSON becoming trace text with an invented default claim; valid arrays bypass field validation. Wrote experiments/verify.md/.json and probes/verify.ts/.json. Proposed independent artifact receipts and a multi-axis proof tray. No browser, model calls or app edits.

- Preference explorer audit: decoded20cases,18successes,216criterion scores and54missingness judgments; only6ordered inputs/4content sets. Every augmented working-prototype option is rotated first, confounding evidence with order. Exhausted1296actual slider settings: zero weights still highlight Pocket tutor, one further nonzero tie exists, and24settings round the leading scores identically. Report separates ordinal index assumptions, semantic assessment validity and subjective utility. No model calls or app edits.

- Original Search reranker audit complete: five queries/six documents/65 recorded judgments; current UI corpus exactly matches wire requests. Runner retention uses relevance plus redirection scores while the UI/live path uses relevance only (13 versus7 questions). A synthetic relevant+redirection document is excluded by runner but retained by actual component. Missing-score fallback fabricates winner100%/others0%; current records are complete. Recorded nested best probabilities render no bars, and stale query completion replaces a newer selection. Wrote experiments/search.md/.json and probes/search.ts/.json. Verified full SciFact test300/corpus5,183 and official BEIR metrics for a proposed full-query evaluation; no download or API calls.

### Dedicated adapters audit, frozen main 4c0c40d5

Read all four implementations, one full shared-response record, runner, tests, ADR and UI. Added `probes/adapters.ts/json`: ten fixtures replayed in each language, six compiler probes, zero provider calls. Confirmed two-level score rubrics become noul, TS raw decode accepts inherited `constructor` while its Zod wrapper blocks it, missing-confidence acceptance differs, and all visible snippets omit required descriptions. Existing TS 4 tests, Rust 5 tests, Go suite and two Python tests pass; cached local toolchains resolved missing shell defaults. Wrote distinct `experiments/adapters.md/json` with repair/P1 verdict, offline conformance plan and semantic workbench design. No browser run or app change.

- Deferred eight new experiment components until their route opens, so the initial laboratory page no longer downloads every new simulation and provider client. Shared Suspense preserves a clear loading state. Static scan of316changed/untracked candidate files found no matches for seven literal secret values read privately from zshrc. This scan does not claim all possible attack vectors are absent.

- Context filter audit: catalog intentionally reuses search evidence. Five decoded cases contain65judgments over336text characters;4/4answerable sources retained and1/1no-source case, with no downstream reader. Actual callback probe shows0%retention still sends all6documents and displays the full-context choice;101slider settings give only2distinct default subsets. Live protocol asks7questions versus13recorded and ignores injection judgments. A late refund result replaces the invoice example while its selector/input remain invoice. Primary RULER config verifies13tasks x500=6500cases per declared length; report budgets a complete4Kfollow-up rather than implying current coverage.
- Fixed a decisions JSON prose-spacing cleanup that had inserted spaces into priority enums; explicit enum/evidence/fix validation and partial report build now pass.

- Micro audit complete: three planned goals, two completed rows, zero completed search episodes. Read all11 wire attempts and five successful calls (11 primitive decisions). The sole drink matches its original menu, but recorded verification receives model predictions/probabilities without menu facts. Actual live callbacks expose billing as the answer instead of its source passage, terminate ask without a question, lose two completed stages when final checking fails, and replace a newer selected goal on late completion. Wrote experiments/micro.md/.json and probes/micro.ts/.json, extending the offline harness to load the real menu. No browser, model calls or app edits.

### Dedicated vision audit, frozen main 4c0c40d5

Read current React UI, historical browser upload/worker/recipe, Python composition, complete recorded/cache/raw row and actual PNG. The source and record match frozen main. Added `probes/vision.ts/json`: image hashes match, one authored task only, no per-stage provenance, and a clearly synthetic merge reproduces repaired answers retaining the old caption/task metadata. Actual Jev chose describe while sufficient evidence was 0.25; no crop/target/action loop exists. Verified ScreenSpot-v2's pinned official annotation files only, about 300 KB: 334 desktop + 501 mobile + 437 web = 1272 tasks across 730 unique image filenames. Zero external evaluations, inference calls or image/weight downloads. Wrote distinct redesign/P1 `experiments/vision.md/json`, with grounded crop/OCR simulation and full external evaluation plan. No app edit or browser run.

- Assigned audit queue validated: changes, routing, verify, original search and micro each have independent Markdown/JSON reports and original probes. All shared-component probes passed after the final harness change; all five report schemas passed. This worker's partial review build completed with 32/33 audits, and git diff --check passed. During handoff another worker updated verification.json to 33/33 with no missing entries; final strict validation remains with the root worker. No application files, model calls or commits were made by this audit worker.
- Strict review builder passed all33original experiments:160findings,19repair,13redesign,1research-map. User confirmed synchronized side-by-side branches and a full game plus crowd. Reused three authorized workers for full Tetris, living courtyard and Ghost Brush. Music route follow-up confirmed14arrangements and no390pxoverflow; first smoke sweep used a wrong CSS selector, not a failed route.

- Integrated suite after live-world additions: 150 tests, 147852 assertions, zero failures across 27 files. Includes genuine crowd/brush evidence checks, game rules, cancellation, replay and BYOK isolation.

- Generated UI browser regression used explicit mocked network responses, no live-model claim: version 1 retained "Branch one draft" and version 2 retained "Branch two draft"; a final JSON event without newline parsed; incomplete EOF was labeled partial; switching to event while a settings reply waited prevented stale replacement; an apartment action created a removable shortlist.

- Smart paste delayed-response browser regression passed: replacing the source during a mocked 1.5-second request kept the new two-line source and all five fields in the waiting state. No old suggestion returned. The first automation attempt used a label locator that did not match this custom Field wrapper; the accessible textbox-name locator completed the same check.

- Release configuration read confirmed canonical project root `jev-experiments/experience-prototypes`, Bun install/build, dist output, outside-root sources enabled and GitHub main production branch. Python urllib lacked its local CA chain; Bun fetch completed the same authenticated read with TLS verification enabled.
- First expanded release scan: 394 changed/new source files plus 728 built files against 15 locally configured literal credentials, zero matches, zero new binary files >=2MB.

- Expanded security review covers browser import boundaries, BYOK gateway/fal endpoints, normalized request receipts, text/SVG escaping and camera cleanup. See SECURITY.md. No source owner keys, authorization artifacts or >=2MB new binaries were found.

- Final integrated code suite after independent-review repairs: 161 tests, 147965 assertions, zero failures; production TypeScript/Vite build passed. Deferred library PURE-comment warnings are benign build annotations, not failed checks.

- Root personally inspected the Tetris, courtyard, Ghost Brush and pixel mobile screenshots. Root browser check replayed the same genuine 12-resident plan into both courtyards, confirmed 0 live/12 recorded decisions per lane, and preserved two earlier branches without horizontal overflow. Tetris retained its 1.1-second game across the benchmark tab and cloned board A into byte-equivalent physical boards.

- Selected changes remain inside jev-experiments. The initial staged snapshot had 414 files and ~68k added lines: about half are 33 individual audit reports, their probes and the generated reader; the major datasets remain JSONL. The staged append-only JudgeBench event file was parsed line by line and ended with a complete newline. The saved application patch uses zero context to avoid legitimate patch context blanks being flagged as trailing whitespace by git diff --check.

- Final scan also includes the local Vercel CLI credential. Adjusted candidate enumeration after staging so it compares against origin/main and includes staged additions, not only unstaged/untracked files. Expanded result: 415 source files, 734 build files, 16 literal credentials, zero matches and no new binary >=2MB.

- Configurable Tetris fallback grace and the explicit 1200ms local slow-reply preset passed the final integrated 166-test suite with 148007 assertions and a production build. Default700ms remains a stated provisional choice; users can select immediate fallback at0ms.

- Production deployment f97ae27 reached READY and published the canonical app. Post-deploy boundary check caught wardrobe-token HTTP500 before the handler: Vercel runtime logs confirmed ERR_MODULE_NOT_FOUND for an extensionless outside-root engine import. Changed it to engine.js, matching the deployed Node ESM contract; all20wardrobe tests and a fresh production build pass. Added check-production.ts to check public assets, no-key401, GET405 and unsupported-model400 without making provider calls. A corrected deployment is being verified before the report is marked published.

- Deployment7438545resolved the extension but exposed a second Node packaging failure: the outside-root engine module did not expose a named export after Vercel compilation. Runtime logs distinguish this from the previous missing-module error. Moved the two public wardrobe constants into experience-prototypes/shared/wardrobe.ts, inside the app ESM package. The research engine re-exports the same contract; the token API imports it directly. Another20wardrobe tests and production build pass. No owner-provider requests were used in these production checks.

- Independent canonical home/Tetris/crowd browser review passed with no blocker. Tetris clocks matched at14180ms, branch restoration recovered the exact previous games, human touch drop worked; the crowd replay reached153ticks in each world and exported matched checkpoint clocks. All three fit390px; no console errors or warnings. Root reviewed production screenshots personally. Detailed source and local-versus-recorded attribution remain in live-worlds/production-review.

- Canonical deployment21edf01passed all9production checks: four public assets200; missing-key evaluate/compose/wardrobe401; wardrobeGET405; fixed-model guard400with a synthetic key. Every API response hadCache-Control:no-store. This confirms the inside-package contract repair on actual Vercel Node, beyond Bun tests.

- Final media polish losslessly remuxed both recorded wardrobe clips, adding finite duration and front-loaded seek cues. Independent encoded packet/timestamp and decoded stream hashes prove no footage or audio changed; original wall-clock capture times remain in the manifests. Two new artifact regression checks bring the integrated suite to168tests,148041assertions across28files, all passing; the fresh TypeScript/Vite build passes too. See wardrobe-lab/media-remux for exact preservation evidence.

- Published8c208b5to the canonical app via clean Git build; deploymentdpl_7PgjRdZoZacfULxwShCZ2PWTUG9NisREADY. All9HTTP/API checks pass, both served media SHA256 values match exact reviewed files, and actual app video plays/seeks forward and backward with a finite29.954srange. Post-deployment evidence files are committed separately from that application revision. JudgeBench recording and its verification/report completion process remain alive; full repeatability results are not claimed complete.

- Final playback-toolbar spacing check: native video controls remain visible at390px; computed gap from toolbar to stage bottom is76px, no horizontal overflow. Root inspected the focused screenshot. Fresh TypeScript/Vite build passed; the earlier168-test suite covers behavior, while this final change only adds a playback class and its CSS spacing.

- Final application revision0584428isREADYon canonical deploymentdpl_DXxfv26m3gKSJy2C8pAhaR5Pzz69. Rechecked all9routes/API boundaries and2exact media hashes, all passed. Canonical mobile player now has76pxclearance above native controls and finite29.954sduration; screenshot/evidence updated. The following commit changes verification documents and screenshots only. Ongoing JudgeBench event collection remains outside this release snapshot.
