# Roadmap addition notes

- The user requested an explicit code-organization and code-style workstream, plus analysis and reimplementation of Jeverative Interfaces. A follow-up added an experiment using Jevframe.
- Keep decision tickets separate from implementation checklists. Code organization is a first-release gate; the new experiments need source research, bounded protocols and working entry experiences before entering the public catalog.
- The composer should use shadcn components, a supplied or user-written style Markdown document, and predefined app-pattern rules. Treat 1–2 seconds as the target for the first usable Jev composition, and measure optional Qwen/Haiku refinement separately.
- Re-read the existing map, architecture notes and package configuration. Python already has Ruff configuration; the app exposes build/test scripts but no formatter/linter scripts. Production code currently spans the app and research folders, so reorganization needs import, artifact and deployment verification.
- The text browser could not load jeverative-ui; an isolated interactive browser loaded Jeverative Interfaces. It credits @hckmstrrahul and describes Jev, shadcn, custom styling and Mobbin-derived rules. These are the site's statements until independently exercised.
- No callable Mobbin connector is available in this session. Official Mobbin documentation describes an authenticated paid-plan MCP; keep access as a research dependency, outside the composition request path. A search-indexed documentation URL returned 404, so cite the working primary MCP page.
- A further request added adaptive interfaces based on marimo-pets. Keep initial composition and adaptation during a task as separate experiments with their own questions and controls.
- Source research pinned JevFrame and marimo-pets and checked original attribution. JevFrame uses hosted SDK calls; marimo-pets supplies contextual notebook companions. Neither source review establishes a ready-made local Jev adapter or a Jev-driven adaptive layout.
- The existing composer already supports batched choices and several previously reported cancellation/state fixes. Re-read current source instead of reopening historical findings. One provider-free mock produced the same tree with two batch calls versus five sequential calls; it establishes call structure only.
- Exercised Jeverative twice: an authored inbox request returned unsupported, then the built-in inbox example produced a preview. Preserved the failure as well as the successful example. The site displayed 1.35 seconds; browser resource duration for that request was 1,712 ms. Click-to-usable timing was not successfully measured.
- Browser instrumentation required two corrections: use URL parsing inside the browser context, and select the outer main element explicitly because the generated preview contains another main. These errors did not require repeating a composition request.
- In the produced local inbox, switching desktop/mobile preview reset selection and lost an unsent draft. Added state preservation to acceptance. Device preview is not physical-device validation. Captures retain the public result and the unsupported state.
- Four decisions and a separate implementation checklist now place code organization/style in the first release and the three experiments in the next wave. No experiment has been implemented or promoted into the public catalog by this addition.
- Independent review found that the composer gates could pass while ignoring valid style instructions. Added matched-task style cases that check compiled tokens, rendered rule adherence and required pattern regions/actions.
- The user added Praneeth Paikray's GEPA adaptation article and allowed folding it into an existing experiment. The repository already has a real GEPA runner and a published bounded negative result. Extend that prompt-optimization experience with a new protocol; do not alter completed local readout training or create a duplicate catalog entry.
- The text browser could not load the GEPA article; a primary-site HTTP read succeeded. Its reported probability/F1 improvement came with additional missed positive cases, making downstream policy outcomes part of the proposed acceptance gate.
- Recomputed the article's paired test metrics from all 600 saved predictions. The new note distinguishes the fixed evaluator from four conversation-assistant prompt proposals, unknown reflection identity/cost, missing optimization payloads and unknown corpus redistribution rights. No model calls or training ran.
- Independent review found no consequential issue in the GEPA decision. Root corrected one source-note phrase that called the earlier proposed replay an existing capability.
- Bundle verification passed local links/anchors, four selected capture hashes/sizes and the four-file incremental patch applied after its design prerequisite. Repeat the checks after any further edit.
- Retained a small GEPA arithmetic checker and its aggregate output/input hashes. The initial standalone Python invocation lacked a usable certificate bundle; adding the system CA bundle kept certificate and hostname verification enabled, and the final check passed.
