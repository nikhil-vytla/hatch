# Portable integration evidence delivery

The isolated candidate is ready for source review. Source commit `3f34e6f0eaed348ece41e41ba13727d9ec64c3c5` is based on remote main `c32e5a69012c30c882485a58c36f81612215ad00`, including the merged foundation and footer. It publishes portable OpenCode, Claude Code and Codex evidence through explicit derivative readers and lineage-aware indexes. No runtime, UI or dependency implementation changed, and nothing was pushed.

[selection.json](selection.json) identifies every selected byte and predecessor replacement. The candidate contains 57 exact files from candidate08 `acd35640945d13bc154765cef2db494026bc1b55` and 4 bounded adaptations: the existing build gets the publication import/call, generated downloads get an ignore rule, a focused CI workflow is added, and the integration README's derivative descriptions/links are corrected. The unrelated atlas UI and its preparation inputs are excluded. All selected files are under 2 MB; no imported third-party source, dependency tree, generated download tree or raw command log is added.

The [lineage comparison](lineage.json) passed for all 47 derivatives across 16 historical conditions. Each predecessor hash, field hash, location binding and event count matches. Outside the explicitly listed location spans, parsed values, event order, returned results, outcomes, usage/cost, unknowns and text-log content are equal. Index relinking preserves chronology and existing gates without regrading. Original context-byte counts and hashes still describe their historical predecessors; portable location references are display data, not replayable commands.

The [exact-archive verification](verification-first.json) passed the canonical `jev-experiments/experience-prototypes` frozen install and production build before any sibling install. The four existing portable/projection suites passed 26 tests and 168 assertions, including stale hashes, unknown envelopes, ambiguous trees, altered gates, mixed original/derivative records and publication failures. Relinking the retained index left its bytes unchanged, and the existing public-record integrity check passed.

Build metadata identified 4 transitive projection inputs, all equal to committed source. The 75 generated downloads, including their index, match their recorded source/output hashes; every input is committed. The new CI workflow runs the canonical build, those same focused tests, index idempotence and public-record integrity. These are local results from the exact source commit; remote CI and deployment remain unobserved.

The [implementation patch](implementation.patch) contains only helper, documentation and build/CI changes. Predecessor identities and replacement names are in the selection manifest; original record bodies and the complete diff remain outside this report. [prepare_candidate.py](prepare_candidate.py), [verify_lineage.py](verify_lineage.py) and [verify_candidate.py](verify_candidate.py) reproduce assembly and verification with explicit isolated paths. Root can review this source condition and publish it separately from the practical execution work.
