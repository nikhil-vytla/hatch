# Portable evidence delivery notes

- Started an isolated delivery candidate for portable integration-evidence readers, derivatives, indexes and publication projections. Runtime and UI changes are outside scope.
- Preserve historical outcomes and original/derivative lineage. Raw comparisons stay private; reports will contain selected identities and test/build evidence. No push, pull request or model call is authorized by this task.

- Fetched remote main into an isolated private clone. The observed base is c32e5a69012c30c882485a58c36f81612215ad00, which includes the footer change and descends from the merged foundation ed52d6d2. The root index and maintained source remain untouched.
- Selected the exact existing candidate08 reader, client schema, index, tests, projection and derivative bytes by its frozen inventory. The isolated source commit is 3f34e6f0eaed348ece41e41ba13727d9ec64c3c5. There are 57 exact candidate08 files and 4 bounded delivery adaptations, plus 47 replaced predecessors.
- Candidate08's full preparation script includes unrelated atlas UI preparation. Kept main's script and added only the portable publication import/call. Added its generated-output ignore rule and a focused workflow with pinned actions and Bun 1.3.14. No dependency, runtime or UI code changed.
- Candidate08's integration README still described only Codex derivatives and linked removed OpenCode/Claude summaries. Updated only those format/link descriptions and omitted the separate unpublished refresh-document paragraph.
- Verified all 47 originals against their derivative source hashes. Recovered each explicitly bound location span privately, checked both field hashes and occurrence counts, then compared all remaining parsed values. Every record/event count, non-location string, outcome, numeric/boolean/null field and text-log remainder matches. The 16-run index differs only by explicit derivative links and lineage.
- Fresh `git archive` of the exact source commit passed the canonical app-root frozen install and production build before any sibling install. The existing four portable/projection test files passed 26 tests with 168 assertions. Index relinking was byte-idempotent; public record integrity passed.
- A separate Bun build metadata graph binds all 4 transitive projection source inputs to committed bytes. Every one of the 75 generated public evidence files, including the index, is reproduced from committed inputs with matching source/output hashes. The selected source condition stayed unchanged after all commands.
- Retained complete diff and raw command logs privately. implementation.patch includes helper, documentation and build/CI changes only; it excludes all original-record deletion bodies and derivative payload copies. No provider or model call, push, pull request or remote CI run occurred.
