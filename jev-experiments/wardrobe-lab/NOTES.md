# Wardrobe lab notes

- Created this folder before implementation. Read the official fal Lucy 2.1 realtime API and playground contract, fal realtime authentication docs, Decart's direct Lucy VTON 3.5 page, and the installed fal client 1.10.1 source/types.
- The selected fal endpoint uses WebSocket signaling plus WebRTC video, with a listed price of $0.02 per second. Decart's direct platform uses a different 3.5 model and SDK; a fal key does not silently become a Decart key.
- The app will send a full cumulative clothing prompt after each accepted edit. It must preserve the jacket when adding sunglasses, then preserve both when changing color or fit.
- No camera or microphone is used during verification. The bounded provider recording uses an original illustrated adult presenter rendered to a canvas stream. Garment thumbnails and references are original vector illustrations, not product photographs.
- Checked the image generation skill because a photorealistic synthetic presenter could help. The built-in image tool is unavailable; no CLI fallback was invoked. The experiment's deterministic illustrated presenter is labeled as such.
- Deployed token handlers accept only the visitor's supplied fal key, authorize one fixed model and never load local owner credentials. Owner credentials are reserved for the local recording script.
- A token probe exposed two concrete integration issues. The inherited FAL_KEY was stale, while the user-designated literal .zshrc key worked. The current /tokens/realtime endpoint requires `app` and `token_expiration`, although the public guide shows `allowed_apps` and `duration`. Verified HTTP 201 and a 70-second expiry from the returned JWT claims without printing the key or token.

- Recorded Jev fixture now includes the requested sequence: denim jacket, sunglasses, “Make them pink”, “Make them bigger”. Pronouns are intended to resolve to the most recently edited sunglasses while preserving the jacket. Four sequence states and eight declared integrity cases run sequentially with checkpointed provider failures.
- First Fal native WebSocket session connected but provider returned an error after 9.3 seconds, before ICE configuration/video. Zero video bytes; recording.json honestly marks unavailable. No camera or microphone was opened. Investigating exact provider error without logging credentials or signaling secrets.
- Vite denied cross-root recorder imports (403). Changed the loopback-only recorder to Bun.build and serve its own browser bundle. No deployed artifact includes the local credential bridge.

- V1 Jev: 12/12 completed, 9/12 exact. V2 explicit referent instructions: 12/12 completed, 11/12 raw exact. Kept both raw records. Pink still selected pink on both jacket and sunglasses. Added a transparent deterministic scope constraint for simple “make it/them …” commands: only the currently focused garment may change. This yields 12/12 guarded outcomes on this same development fixture, not held-out accuracy. UI displays the discarded jacketColor proposal and raw response.
- Second Fal attempt succeeded: 30.0 seconds / 904,639 bytes of actual remote video, 33.4-second connection, three set_image_ack events after cumulative updates. Model states came from authored demoOutfits, not Jev responses; the clip and UI explicitly distinguish that provenance. The full first unavailable attempt remains recorded.
- Reset/removal must replace the old reference image, not omit reference_image_url: omission can leave the last garment reference active. A base T-shirt reference is now sent even when no outerwear/accessories remain.

- The authored control clip is real Lucy output, but it is not an end-to-end Jev demo. Added synthetic speech using macOS say and local MLX Whisper. Tiny.en misheard “them” as “some”; default-voice base.en also missed pronouns. Those transcript trials remain alongside the final evidence. A synthesis script initially used zsh's special `commands` array, producing empty audio; fixed with task-specific variable names and retained the empty transcription diagnostic. Samantha at 145 wpm + base.en transcribed all four original phrases correctly; no transcript text was corrected by hand.
- All four final transcripts exactly match previously recorded v2 Jev inputs, and each guarded output exactly matches the next input state. The spoken-pipeline recorder reuses those actual model responses only after exact text/state equality; otherwise it calls Jev. Each row names its reused source and original inference latency. This was explicitly authorized by root. A new 30-second Fal capture schedules those actual accepted states after replaying the original synthetic WAV utterances; its media includes a real audio track, separate from the authored control clip.

- Final spoken artifact: 30.0035 seconds, 994,156 bytes, 720×1280, VP8 video + Opus audio, 32.971-second provider connection. Frame review at 4/13/21/29 seconds confirms navy jacket preservation and black-to-pink eyewear. The bigger-frame request is not conclusive visually; sleeves/coat proportions/pose drift, trousers shift to cream at an intermediate sample, and unrequested shirt markings appear. Recorded those limitations in spoken-recording.json and the UI/report. No canonical-state score is being used as a pixel-quality score.
- Browser QA on production preview 5195: all four recorded edits produce navy denim + pink large wayfarers; Undo restores regular pink frames while preserving the jacket. Manual bomber selection remains legal. No-key live submission preserves input and explains missing Jev key. Video connect stays disabled without supplied fal key and explicit consent. No camera/microphone was requested.
- Dark mobile layout at 390×844 and reduced-motion preference checked, with no horizontal overflow. Fixed a clipped provenance label inside the rounded stage and adjusted the avatar crop. Actual spoken clip decodes at 720×1280 and exposes one audio track in the browser. Browser console had no runtime errors (only a password-field-outside-form browser hint). Screenshots are in output/.
- Final validation: 20 Bun tests, 104 assertions, TypeScript clean. Scanned all 61 investigation files, including browser diagnostics and binaries, against actual available fal/gateway credential values: zero matches. The local bridge is not deployed. Dependencies/model weights are cached outside the investigation folder. No commits were created.
- Final ambiguity rule: if one edit actually changes both garment slots, clear focus rather than carrying an old referent into the next pronoun command. Regression test added; final total is 20 passing tests / 104 assertions. Final metrics remain 11/12 raw, 12/12 guarded.
