Parallax's second synthesis strategy, checkpoint evolution, now runs end to end offline and is one approval away from its first paid screening: an agent extends its own persisting workspace across an ordered checkpoint sequence while sealed test obligations accumulate, and checkpoint N is gated on checkpoints 1..N-1 still passing. The slice follows the mechanics of [SlopCodeBench](https://arxiv.org/abs/2603.24755) (per-stage strict/isolated/core verdicts, automatic regression reclassification, black-box entrypoint-only grading, continue-on-failure with censoring) with strict frozen Pydantic models, digest-chained evidence receipts, and validators that make ungated or partially delivered episodes unrepresentable. The screening prerequisites are implemented: a provider adapter maps the existing [HUD-gateway boundary](https://github.com/nikhil-vytla/hatch/pull/27) onto the pure agent function with per-stage token/cost metering, and every model-written case executes in a digest-pinned Docker container with no network, a read-only rootfs, a non-root user, and resource limits — with no host fallback on the live path. No paid inference ran.

- 197 offline tests pass (73 for this slice), plus a twenty-four-mutant behavioral gauntlet fully killed — including sandbox-bypass, network-isolation-drop, obligation-drop, and spend-approval-drop mutants.
- Committed dry-run evidence exercises the full screening path (manifest, both arms, delivery, adapter wire models, verification, receipts) with a scripted gateway at $0, once host-side and once through the real container sandbox.
- The preregistered 60-call Haiku screening is launch-ready: worst-case cost bound under $1 against a $5 hard cap, blocked only on user approval and a valid HUD API key.
