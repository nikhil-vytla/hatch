A durable model gateway and `strive.harness/1` adapters now connect opencode, Codex and Claude Code to the unchanged M1 through M3 core, with all qualification generations driven by local fixtures. The tests compare opencode/Codex proposals, accounting and recovery, enforce a single dispatch, and close the durable request/result probe through real Deno processes. Native CLI qualification and funded smoke tests remain deferred because this host rejects the required OS jail; the report separates those limits from the enforced fixture controls. Milestone 5 should begin with an atomic operation journal and verified lookup for the pinned [tau2-bench](https://github.com/sierra-research/tau2-bench) telecom workload, using the [qualification report](README.md) as the integration boundary.
