- Read M1 commands, records, lifecycle/recovery tables and M2 CAS, writer, codec, and pure verifier. M2 already preflights every append, holds the lease, and advances epochs on reopening.
- Host is macOS 26 / Darwin 25, with sandbox-exec and Deno installed. Checking actual sandbox availability before claiming OS isolation.
- M2 only permits a new effect after the prior result is consumed. Return records can retain usage evidence before a separate settlement. Cross-epoch observations require explicit reconciliation evidence.
- The frozen verifier cannot discard a pending command via a continuation. Admission validation therefore needs to reject invalid candidate commands before acceptance, with authorization rechecked immediately before dispatch. No verifier/schema changes planned.
- sandbox-exec exists but sandbox_apply fails with Operation not permitted inside this session. No Seatbelt guarantee is claimed. Deno 2.9.5 is available. Candidate execution will use a trusted, import-free JS bootstrap and evaluate candidate source only after Deno permission checks apply; candidate code must never be the initial module graph.
- Official Deno docs explicitly exempt the initial static import graph from permission checks. Runtime dynamic evaluation avoids that exception. No host read/write grants are needed: scoped input handles carry copied bytes.
- Production OS memory/storage/process-tree enforcement remains separate from Deno language permissions. Broad acceptance probes mentioning external harness launch/forward cannot honestly close before M4.
- Papercut logging is not opted in at the parent git root; no external file was created.
- uv run --offline with a fresh in-repo cache panics in macOS system-configuration before type checking. Trying --no-sync against the existing environment avoids dependency resolution and writes outside strive.
- Implemented broker capabilities pinned by CAS policy reference, pre-dispatch admission, cumulative trusted receipts, and a ledger derived exclusively from VerifiedState. No mutable accounting cache or verifier/schema changes.
- Implemented a one-use in-process upstream gate. Its callback preflights and commits actual request bytes/stage authorization before forwarding. Real harness launch, gateway token revocation, durable spool and provider qualification remain M4.
- First runtime suite: 25 passing recovery/accounting/attack tests. Expanded to 66 passing tests plus one strict-xfail after integrating charged candidate steps.
- /bin/ps cannot execute in this managed session. Darwin proc_pidinfo on an owned child succeeds; RSS sampling now uses libproc, with the struct layout checked against the installed macOS SDK. This is a watchdog, not a hard allocation ceiling.
- Sandbox attacks pass for env credentials, authority storage, protected evidence, host writes, network, subprocess, FFI, static source imports, runtime imports and worker imports. Candidate source is dynamically compiled behind the Deno permission boundary, never an initial module graph.
- Test fixes: port 9 triggered Fetch's blocked-port rule before the permission check, so the test now uses port 8123 and requires a permission denial. Worker load errors are asynchronous, so the test waits for its error event. A permanently unresolved promise exits Deno by itself; the wall-limit attack now keeps a timer active.
- Candidate time accounting uses a supervisor-owned runtime.step effect. A continuation consumes the prior external result into a pending sandbox request carrying that exact result. The next sandbox result is consumed with the candidate's StepOutput. This fits the frozen serial verifier while reserving sandbox time before execution. Unsupported interruption suspends with its wall reservation intact.
- Full strict mypy passes, 84 source files. uv run --no-sync is necessary here to avoid uv's macOS resolver panic; no dependencies were changed.
- Final code review fixed recovery routing: adapter code identity alone cannot select between differently bound instances. Recovery now resolves the binding through the accepted canonical command.
- Confinement profiles now include runtime/monitor hashes and every resource setting. Pending invocation resume rejects changed heap/output/input/CPU/RSS/wall limits, and later steps cannot silently change the profile.
- Added actual file credential, raw TCP, Unix socket, RSS allocation, changed-profile and forbidden input-projection attacks.
- Full suite: 532 passed, 5 xfailed, 2 failed in 194.25s. Both failures are unchanged legacy packaging tests, whose uv build subprocess panics before building in macOS system-configuration. The same two failures are documented in the M2 report. Proxy/offline flags do not avoid the panic. The full run collected before the final six added cases; those are covered by the final focused run.
- Cached Hatchling wheel build succeeds without uv resolution. New runtime Python and JS resources are packaged; importing the extracted wheel and invoking its legacy sandbox CLI succeed. The reusable check_wheel.py records this separate smoke check; it does not substitute for the failing isolated-install tests.
- Late overrun after a new command is accepted exposed an M2 constraint: suspension cannot discard that command. Runtime now retains the pending command and honors the durable dispatch_stopped fence during recovery. Its execution_status may still be CONTINUE, but drive cannot dispatch. A regression test passes.
- Final strict mypy passes on 85 files, including the wheel-check script. All M1 contracts and M2 authority code remain unchanged.
- Final focused verification: 239 passed, 5 xfailed in 13.02s. git diff --check is clean. Removed all generated work caches, test stores, wheels and extracted package files. Only source/tests, review documents and the diff of existing-file edits remain for review. No commit was made.
