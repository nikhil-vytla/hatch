Milestone 8 adds a reproducible Linux container definition and a candidate/harness jail built with [bubblewrap](https://github.com/containers/bubblewrap) and cgroups v2, including hard memory/pid limits, default-deny seccomp, read-only runtime files and a bounded scratch mount. Runtime probes control enforced-versus-deferred reporting, and the existing authenticated gateway pipe is the only harness egress route. The isolated [tau2 adapter](https://github.com/sierra-research/tau2-bench/commit/a2c024725189473d2d7cea3a5cfdbcc67478e41f) gains pinned installation, full inventory certification and offline evaluator/snapshot/crash-recovery tests using authored responses. All 30 frozen core files remain unchanged, and the full macOS vNext suite passed 443 tests with 21 skips and one expected failure; Linux enforcement and live tau2 certification await the orchestrator's container run.
