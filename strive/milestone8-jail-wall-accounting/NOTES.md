# Milestone 8 jail wall accounting notes

- Scope: fix Linux jail startup versus candidate execution wall accounting; preserve host behavior and confinement. User reports 488 Linux passes and one tiny-wall-budget failure; this macOS environment cannot run the jail.
- Created this investigation folder before inspecting code. Do not commit. Keep frozen core/contract/verifier/ledger/gateway and tau2/codec work untouched.
- Inspected the retained Linux failure in `.container-results/pytest.txt`: `_capture` first raises the correct wall-limit `SandboxFailure`; `LinuxJail.close()` then raises raw `TimeoutExpired` from `self.process.wait(timeout=5)`, masking it. A launch timeout can kill an empty cgroup before `_enter_cgroup.py` has joined, leaving the launcher alive.
- All 30 frozen hashes match before edits. `runtime/sandbox.py` and `_limits.py` are frozen; keep them untouched. The existing trusted JS bootstrap can receive a jail-only prefix in its temporary copy without altering its on-disk bytes or the frozen wire contract.
- Chosen mechanism: trusted Deno bootstrap writes a fixed stdout ready prefix before any candidate code. The jail capture loop strips that prefix once, then starts the candidate wall deadline. Startup gets a separate generous bound. No extra descriptors or Deno permissions are needed; subsequent candidate output cannot reset the deadline.
- Cleanup will keep cgroup.kill and also kill the launch session, covering the pre-attachment race. Any post-kill wait failure must become a distinct jail cleanup error rather than raw TimeoutExpired.
- Tooling friction: Git fsmonitor socket failed and an unquoted zsh glob missed. Papercut logging was attempted, but this repo has not opted in; no PAPERCUTS file was created.
- Implemented a jail-only ready prefix in the temporary trusted bootstrap and a 30-second startup deadline. The first complete prefix starts the unchanged candidate wall budget; output accounting strips only the trusted prefix and counts all later bytes.
- `LinuxJail.kill()` now kills the launch process group first, then the cgroup, preventing a late launcher attachment after an empty-cgroup kill. Cleanup converts a post-kill wait timeout into `JailUnavailable` and clears the reaped process handle so repeated close is safe.
- Added cross-platform supervision regressions using real Deno/process sessions plus a delayed launcher, and two native-jail regressions for the Linux rerun. No container execution attempted on macOS.
- Exact `uv run mypy --strict` hit the known uv system-configuration NULL-object panic before mypy. The first --no-sync check caught a test-only optional-timeout annotation, now corrected.
- First targeted run: 47 passed, 11 native-Linux skips, one test-cleanup failure. The injected cleanup-timeout test issued a redundant second killpg after production cleanup had already killed the child; macOS denied that second signal. Removed the redundant test signal and retained the real wait/reap.
- Strict mypy via --no-sync is clean for 180 source files. The 0.4-second delayed-start tests demonstrate quick-candidate success and graceful 0.2-second wall failures for the 10-second sleeper, top-level busy loop, and candidate that closes both output pipes.
- Targeted sandbox suite completed: 48 passed, 11 skipped in 13.65 seconds. All skips require real Linux confinement. Started the full host suite, including legacy tests, with `uv run --no-sync pytest -q -ra`.
- Scope audit confirms only confined_sandbox.py and linux_jail.py changed among the existing source/test/adapter/script files; added test_confined_sandbox_wall.py. All 30 frozen hashes still match.
- Saved a git diff --no-index patch against pre-edit copies because the two runtime files were already untracked Milestone 8 additions. The patch includes only this task's delta plus the new test; reverse-apply check passed. Baseline copies live outside the report folder.
- Final cleanup review added a guard against signaling an already-reaped launch PID, which could be reused. The shim cannot fork before cgroup attachment, so surviving descendants after launcher exit are still covered by cgroup.kill. Added a regression proving that the cgroup kill still occurs without signaling the old PID; rerunning affected suites and mypy.
- Final affected-suite rerun: 49 passed, 11 Linux-only skips in 15.40 seconds. Strict mypy is clean for 180 source files after the final cleanup guard and its regression. Full host suite was already running when this final guard was added; the affected-suite rerun covers that delta.
- The broad host suite hit two legacy packaging failures. A focused rerun confirms both stop in `uv build --wheel` with the same macOS system-configuration NULL-object panic, before building any code. This is an environment failure outside the jail change; retained details in pytest-packaging.txt. The rest of the suite continues.

- Full host run finished: 785 passed, 24 skipped, 1 expected failure, 2 failed in 1132.81 seconds. The only failures are the two legacy packaging tests whose `uv build` stops in the macOS system-configuration panic. No vNext tests failed. Per-group counts are in host-results.json.
- Final scope audit and reverse-apply check pass; all 30 frozen hashes match. Source/test hashes match the retained patch and scope report. No commit created. Native Linux validation remains explicitly pending the orchestrator rerun.
