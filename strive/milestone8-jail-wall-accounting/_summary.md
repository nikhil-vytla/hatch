The Milestone 8 Linux jail now separates startup from candidate execution using a trusted [Deno](https://deno.com/) ready marker and a 30-second startup deadline. A 0.2-second wall budget still kills a ten-second candidate through the normal `SandboxFailure` path; cleanup also kills the [bubblewrap](https://github.com/containers/bubblewrap) launch session to cover the race before cgroup attachment. The targeted host suite passes 49 tests, strict mypy is clean for 180 files, and all 30 frozen hashes match; the broad host run passes 785 tests, with two packaging failures caused by the macOS uv panic. Native Linux validation remains for the orchestrator rerun, with two new regressions checking slow startup and complete cleanup.
