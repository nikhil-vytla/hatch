# Langfuse profile trim notes

- Created this folder for the requested release 1 telemetry trim. The user explicitly prohibited commits, PRs, and history changes.
- Scope is telemetry profiles, manifest validation, related tests and documentation, and the single affected core-freeze hash if needed. Verifier, ledger, gateway, broker, supervisor, and admission logic must stay untouched.
- Initial repository status showed no existing changes. Git emitted an fsmonitor socket error; subsequent inspection will disable fsmonitor for each command.
- Confirmed branch `strive-astra`. The git root is the parent `hatch` folder; all task writes stay in `strive/`. The parent AGENTS.md matches the supplied instructions.
- The papercut CLI declined to log because the repository has not opted in. No log was created.
- The freeze test reads `m5-investigation/second-benchmark-core-freeze.json`, which includes `contracts/manifest.py`. M6/M7 integrity JSON files are historical verification snapshots, not the active baseline.
- Removed the two deferred mappings and enum members. Added a targeted manifest error naming the unsupported value and the shipped Langfuse option. Both authored and resolved TOML loaders are covered.
- Kept the outage, rebuild, duplicate prevention, journal expenditure, metrics, and execution dependency tests. The Langfuse case also checks that neutral attribute keys survive export.
- Updated design section 8.2, the release scope and workflow tables, and Amendment 1's observability reference. All current manifest examples and fixtures already select Langfuse; historical M7 reports remain records of that earlier work.
- Refreshed only the manifest baseline entry, as explicitly authorized: `36ae490a85795a4c58a29535b4bd44799c4a105263a4bacf1535c306f9a1fc44` -> `7cfe64182077324efb3f9818f7933f403689e7b23810de51b1788490a8bc55f4`. Verified the other 29 frozen files still match their original entries.
- Initial `uv run` validation attempts stopped before collecting tests or type checking because uv's default cache was outside the writable area. Rerunning with `UV_CACHE_DIR` inside this folder, following the existing M7 setup. Before retrying, corrected the test's lookup of exported spans to account for the exporter's destination/span-ID tuple keys.
- A local cache alone still hit uv's existing macOS `system-configuration` panic. M5/M7 notes document this host issue and the working setup: `UV_NO_SYNC=1 UV_OFFLINE=1` with the installed environment. Applying those environment settings without changing dependencies.
- Strict mypy passed across all 168 configured source files. CLI help advertises only `--profile {langfuse}` without requiring a CLI source change.
- Compared the baseline JSON against HEAD: exactly one changed entry, still 30 files, all current hashes matching. Verified zero diff against HEAD in verifier, runtime, or the harness gateway.
- Started the full vNext suite with JUnit output and all pytest temporary files under this folder. The previous M7 full run took about 17 minutes, so the full-suite count remains pending until this run exits.
- The full run exposed a second freeze guard in `tests/vnext/test_harness.py`. It compares the historical M4 baseline to the active M5 baseline and permits only three Amendment 2 changes. Added only `contracts/manifest.py` to that exact approved-change set, with the human-approved telemetry trim documented beside it. The historical M4 baseline remains intact; all other paths are still checked. No harness implementation changed.
- First full run completed in 924.51 seconds: 440 passed, 10 skipped, 3 xfailed, and the one older freeze-guard failure. `ps` inspection was denied by the sandbox; pytest's own logs and JUnit output identified the failed case. Strict mypy passed again after updating the guard.
- Repeating all 454 cases on the final code in four independent pytest processes with separate temporary directories. The small `rerun_vnext.py` runner collects the entire suite, uses prior timings to distribute cases, and asserts that every collected case appears exactly once in the combined results. No test is omitted and no dependency is added.
- The focused verification passed all 9 cases in 27.15 seconds: both freeze guards and all telemetry tests, including both deferred-profile rejection cases. The final full-suite rerun is in progress.
- The first sharded attempt exposed a runner setup mistake: pytest does not create parents of `--basetemp`. It produced 316 setup errors because `final-tmp/` did not exist, alongside 130 passes, 6 skips and 2 xfails. The runner now creates that parent before starting subprocesses; all 454 cases are being rerun. No application change resulted from this setup error.
- Final full rerun succeeded across all 454 collected cases, each executed exactly once: 441 passed, 10 skipped, 3 xfailed, 0 failed. All four pytest processes exited 0; their durations were 253.84, 242.71, 252.30 and 257.76 seconds.
- Parsed the combined JUnit evidence to confirm guarantees 2–5 and both freeze guards passed. The xfails are exactly native harness confinement for guarantee 1, the production OS confinement floor, and deferred EvaluateFork.
- Rechecked all 30 hashes, the single-entry baseline diff and the untouched verifier/runtime/gateway paths against HEAD. Saved the six-file patch and final README and summary. No commit, staging, PR or history change was made.
- Removed generated pytest execution stores after verification; retained only the small reports, logs, test manifests, runner and patch.
