# M7 work notes

- Started M7 on `strive-astra`. Scope is the research workflow, with no commits or changes to frozen core files.
- Reading the design, manifest contracts, runtime, benchmark, policy, and existing tests before choosing integration points.
- The frozen manifest codec already handles `ResolvedManifest`; no authority schema additions are needed. M7 records resolved manifests as ordinary retained artifacts behind `RunBinding`.
- Composition uses the M5 counter adapter, M6 `ContinualRefine`/`BundleManager`, real Deno execution, `GatewayRefiner`, and the existing supervisor/broker/ledger. First smoke run finished with trusted scores 0 then 1.
- Corrected producer identities during setup to match the verifier's bound runtime/gateway/adapter/scorer pins. Counter initialization accepts exactly `{}`; workload seeds select the order independently of policy/model calls.
- Study allocation is published before any setup/dispatch. All repetitions resolve before the first dispatch, with local authoring inputs snapshotted for interrupted preparation. No allocated identity is replaced.
- Audit uses separate run roots and CAS, scopes, environment/operation databases, grants, bundle registries, retrieval/cache/conversation directories, model gateway spools, budgets and export destination. Workflow leases fence freeze against active sessions. Frozen development sessions cannot resume.
- Reports read trusted measurements and journal usage. Missing outcomes have explicit counts and bounds, separate from complete-pair intervals. Repeated development exposures support an explicitly limited retention comparison; unmeasured corpus items remain visible.
- Read OTel GenAI 1.41.0 agent/model/metric definitions and official Langfuse, LangSmith and Phoenix mappings. The projector is a separate consumer; no runtime, policy, benchmark, store or verifier module imports it.
- Frozen records have durations but no absolute timestamps. Telemetry uses a labeled relative duration axis derived from broker observations; it never substitutes export time for execution time.
- Initial workflow test pass: 10 passed, 1 failed. The failure correctly rejected a resume because I edited retained runtime source while that test was running. Subsequent focused runs keep source fixed.
- Initial audit tests caught an invalid test assumption: a protected run can independently produce bytes identical to public development bytes, so CAS digest equality is not evidence of leakage. The test now uses an audit-only observation for absence, and compares the complete adaptive payload stream across protected-target variants.
- Git fsmonitor failed locally; read-only Git commands use `-c core.fsmonitor=false`. The papercut CLI reported the parent repository has not opted in, so no papercut log was created. uv's default cache was outside the writable sandbox; checks use this folder's `.uv-cache` with `UV_NO_SYNC=1 UV_OFFLINE=1`.
- Focused run passed all 19 tests in 238.93 seconds, including protected-feedback noninterference. The combined guarantee-2 acceptance driver now runs both the existing forged-facts probe and the new campaign noninterference probe; its xfail has been removed.
- Added campaign/arm context before dispatch, independent-run campaign cost rollups, and separate actor/controller package ownership. Strict mypy remains clean across 165 source files.
- Final read-only review identified three follow-ups: sync the parent of every outer run directory, retain Deno executable bytes in the resolved closure, and project observed identity from the authenticated gateway evidence. The sandbox rejected SIGINT to the first full-suite process, so that run is being allowed to finish before the final implementation/check pass.
- First full vNext pass: 433 passed, 10 skipped, 3 xfailed in 867.53 seconds. Legacy CLI: 5 passed in 9.25 seconds.
- Applied the review fixes after that pass: outer run-directory fsync; retained Deno binary, initial memory and both sides of tool schemas; observed-model/provider-response extraction from authenticated M4/M6 gateway receipt formats; exact audit allocation conversion independent of Decimal context; honest indeterminate status for stopped pending work; root-argument-safe CLI routing. Audit HTTP export now requires a distinct declared development endpoint, in addition to the frozen release marker and separate destination namespace.
- Strict mypy after the review changes: no issues across 166 source files. All 30 frozen hashes still match, and the frozen core/gateway diff remains empty.
- A deterministic live-reader probe reproduced a real inspection race: a legitimate append between the M2 reader's final frame and final stat raises `journal changed during verification`. Preparing an inspection-only captured-prefix reader, still passed through pure replay, with separate diagnostics for concurrent change and incomplete tails. Committed corruption must still fail, and no recovery, truncation or writer lease is permitted in inspection. Source changes are waiting for the active full-suite run to finish so pinned-runtime resume tests see stable source.
- Final report review: fulfilment bounds must include every planned outcome without a trusted measurement, including infrastructure failures and exclusions. Those coverage categories cannot implicitly become zero-valued scorer observations.
- Second full vNext pass: 438 passed, 10 skipped, 3 xfailed in 941.94 seconds. The inspection-only snapshot consumer then passed 5 focused checks; strict mypy remained clean across 168 files. Snapshot replay preserves core corruption checks and reports live append/partial-tail diagnostics. Outcome bounds now include all unmeasured planned outcomes.
- The installed CLI smoke pass returned zero for run, resume, fixed run, matched/descriptive compare, status, experiment and in-memory project. Added an interrupted-setup case where authority exists but the manifest has not committed; this must remain an indeterminate allocated row without new dispatch. The final source is being held fixed for the last complete suite and installed-command smoke pass.
- Final-source focused checks: 4 passed, 16 deselected for concurrent inspection, partial-tail corruption handling and both interrupted-setup cases. Strict mypy passes across 168 source files. Final-source installed CLI smoke: all 8 commands returned zero. The combined guarantee-2 probe has passed again during the final complete suite. A 27-file hash snapshot confirms no source changed during these checks.
- Final full vNext suite completed: 441 passed, 10 skipped, 3 xfailed in 1004.19 seconds. The xfails are exactly native-harness confinement, the production OS confinement floor and deferred EvaluateFork enactment. Guarantee 2 passes, including both its forged-facts and protected-feedback halves. The ten skips retain their OS, funded-provider and tau2 installation/inventory reasons in the final log.
- Final read-only audit: 30/30 frozen hashes match, the frozen core/gateway diff is empty, all 27 verification source hashes match, and `git diff --check` passes. Branch remains `strive-astra`; no commits, PRs or history changes. README, source inventory, test evidence, diffs, examples and the requested concise summary are complete.
