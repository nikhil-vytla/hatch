# Amendment 3 work notes

- Started from the existing `strive-astra` worktree. Preserve all pre-existing changes, especially the verified Linux jail and MultiToolMessage codec fix. No commits or history changes.
- Scope: base-scenario grouping, a derived adaptive whole-group split, a separate initial fixed-agent stock-test mode, certification gates, tests, and design documentation.
- Host restrictions: do not install or execute tau2 or run containers. Use captured evidence and host-only tests; retain Linux verification instructions.
- Initial `git status` hit an fsmonitor IPC warning. Use `git -c core.fsmonitor=false` for subsequent read-only inspection. Root `AGENTS.md` is not on disk; the user supplied its instructions directly.

- Confirmed the Git root is `/Users/nikhil/personal/hatch`; all work is within its `strive/` subtree. Moved the initial investigation folder up one level accordingly. Parent `AGENTS.md` matches the supplied instructions. No papercut log exists under the allowed write scope, so no log was initialized.
- All 30 core-freeze hashes matched before changes. Retained starting hashes and editable-file baselines for an incremental patch and final scope audit.
- Inspected the pinned TaskManager source. Its `[name]` base template is decorated by a composed failure-condition name and a PERSONA suffix. The condition list is not an ancestry path; selecting its first alphabetic token would invent finer base seeds. Implemented direct template-root grouping and exact persona-free scenario equality only for non-generator contract fixtures.
- Replaced stock-split gating with a deterministic dynamic program over reachable dev/val sizes on the selected base pool. It allocates every selected ID exactly once and minimizes absolute distance from 60/14/40. Stock base/train/test source integrity remains checked.
- Grading is still blocking for every selected task, including all assertions, reference execution, unknown components, missing criteria, and NL assertions. Unselected grading findings and both direct-root and historical transitive stock-overlap diagnostics are retained as informational records.
- Shell source retrieval failed DNS. Used the web reader for pinned split IDs and generator/config/runner documentation; no tau2 package, container, or upstream code was executed. Upstream docs say `base` is the original leaderboard denominator. The requested fixed `test` mode must therefore qualify comparability as stock-test only, not claim equivalence to a 114-task base leaderboard.

- The published base IDs resolve to three root groups of 49, 36, and 29 tasks. A pure size-error objective alone preferred 65/0/49, eliminating validation. The implemented three-way allocator keeps each partition nonempty whenever at least three groups exist, then finds the global size-error optimum. It yields 49/29/36, with all 114 IDs retained. Fewer than three groups explicitly report empty partitions rather than silently dropping tasks or failing the size target.
- Retained `derived-split.json` and `analyze_evidence.py`. The derived split uses all 114 published IDs. Its full crossing-ID map comes from the captured 2,285-ID finding, not a new host inventory scan. Linux certification will retain the complete task inventory and new live scan.
- Added `fixed-stock` qualification, mode-specific adapter split declarations, version-2 certificate loading, and a standalone initial upstream LLM-agent runner. The runner pins user model/temperature, stores its initial actor artifact and resolved config, disables adaptation, and requires complete unique task/trial results. It deliberately labels the supported actor implementation; it does not pretend an opencode actor was ported.
- Host focused pass: 48 passed, five live tests skipped, 12.66 seconds. Strict mypy passed 179 files with `UV_NO_SYNC=1 uv run mypy --strict`. A duplicate-trial regression was added after this collection and is checked separately below.
- Plain `uv run mypy --strict`, including an offline attempt, still panics in macOS `system-configuration` before launching mypy. Both unchanged packaging tests reproduce the same panic inside `uv build`. An explicit offline/proxy environment probe also panics. These are existing host tool failures, not passing validation; no packaging test was weakened or skipped.
- Started the complete host suite and retained JUnit/text output. Updated the Linux result checker to require the added fixed-stock live test. No jail implementation or runtime/core source was edited.

- Final focused run after TaskSpec root-identity alignment and duplicate-trial checks passed 79 tests, with five live skips, in 31.49 seconds. Strict mypy remains clean for 179 files.
- Restarted the full host suite after the last implementation edits to avoid mixing already imported fixture code with updated subprocess code. The interrupted run is retained separately. An attempted process listing was denied by the host sandbox; the tool's session interrupt stopped the test run cleanly enough to restart. No sandbox settings changed.
- Scope audit checked 42,966 starting files and found only the intended 11 edits and three new source/test files. All 30 frozen core hashes match. All pre-existing `src/strive/vnext` bytes, jail setup/entrypoint bytes, native worker codec fix and its regressions are unchanged. Compacted the retained baseline to 237 source/config hashes; the full hash list remains in the ignored scratch baseline.
- Saved an incremental Git diff against the initial worktree and verified it with reverse `git apply --check`. The first patch-check attempt ran before the background audit finished; the retry succeeded after completion.

- Final complete host run: 772 passed, 22 skipped, one expected xfail, and two failed in 1,136.43 seconds. JUnit confirms the failures are only the two unchanged packaging tests, both caused by uv's macOS SystemConfiguration panic. All five live tau2 tests skipped on host. No fully green host-suite claim is made.
- Post-suite scope audit again confirms all 30 frozen hashes match, no protected/core/jail/codec bytes changed, and the incremental patch passes reverse-application checking. No commits, PRs, or history changes were made.
