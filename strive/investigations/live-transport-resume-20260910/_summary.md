OpenAI upstream HTTP now runs on an eagerly started egress thread, so a gateway dispatch thread that enters a per-thread Linux network namespace cannot strand the provider call. Connect waits are capped at 10 seconds and connected calls at 60 seconds within the shared generation deadline; errors retain their sanitized cause in SQLite and stderr, and ambiguous operations still suspend with their reservation retained. Deterministic tests exercise the boundary, blocked DNS, late-connect cancellation and restart without redispatch, with a real Linux netns test ready for container qualification. See the [transport implementation](../../src/strive/vnext/harness/openai_live.py) and [Podman proof runner](../../live-tau2-budget-proof/run-container.sh); the historical incident still needs container confirmation.
