The tau2 campaign's [OpenAI transport](../../src/strive/vnext/harness/openai_live.py) already executes outside the candidate jail, but it discarded transport causes, capped response reads at 60 seconds, and ignored proxy routing used by urllib. The fix records sanitized failures durably and on stderr, shares the existing generation deadline across counting and generation, supports trusted HTTP CONNECT routing, and preserves upstream errors after a child pipe closes. Deterministic tests exercise the real transport without spending, including a 90-second response and campaign suspension with its reservation retained; all 30 frozen-core hashes remain unchanged. The exact cause of the previous container failure cannot be recovered from its generic error, so the orchestrator must rerun the unchanged $0.05 proof using the [documented command](README.md).
