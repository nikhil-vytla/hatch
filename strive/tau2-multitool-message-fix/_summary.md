A missing outer `role="tool"` caused recorded tool batches to fail reconstruction in the pinned [tau2-bench model](https://github.com/sierra-research/tau2-bench/blob/a2c024725189473d2d7cea3a5cfdbcc67478e41f/src/tau2/data_model/message.py#L519-L565). The adapter now constructs batches through that model and strips only message-envelope timestamps, retaining required fields and tool payloads. Nine new host regression cases passed, strict mypy passed with `--no-sync`, and all 30 frozen core hashes match. The full host suite finished with 761 passed, 21 skipped, 1 xfailed, and two packaging failures caused by uv's macOS configuration panic; Linux confirmation remains with the orchestrator, and no commit was created.
