# Milestone 5 implementation notes

- Started by creating `strive/m5-investigation/` as requested. Reading Amendment 2 and the existing runtime before implementation.
- Work is limited to the supplied strive repository. No commits, history edits, or PRs will be made.
- The workspace is already the `strive/` subtree of the parent Git repository; moved the investigation folder to its root. Source changes remain in this subtree.
- Git fsmonitor IPC warning is bypassed with per-command `-c core.fsmonitor=false`; papercut logging declined because the parent repository is not opted in.
- Added pinned scoped admission, keeping exact-hash capabilities intact. Trusted staging grants bind bytes to scope, source and current environment; recorded result projections are readable but receipts/state and candidate labels do not grant access.
- Added atomic suspended operator restoration through RevisionActivation. The verifier preserves all state except active bundle/revision; supervisor refuses candidate RestoreBundle and never drives suspended work.
- Changed existing test `test_operator_restore_uses_durable_command_preserves_accounting`: fault boundary ACCEPTED -> ACTIVATED, and assert continued suspension, because suspended restoration now commits atomically without accepting a new command.
- Isolated `uv lock` panicked in macOS system-configuration (`Attempted to create a NULL object`), before dependency resolution. Direct task retrieval failed DNS; web retrieval exceeded the reader's 4MB limit. No live tau2 execution claimed.
- Plain uv commands cannot write their default cache; `UV_CACHE_DIR=/tmp/strive-m5-uv-cache uv run --offline --no-sync ...` uses the existing environment. Initial strict mypy passes.
- Core broker/bridge/store and first telecom fixture tests now pass: 16 benchmark tests, 13 qualification/closure tests, and 5 direct user-model tests. The fixture backend is explicitly synthetic and is not evidence of live tau2 execution.
- Guarantee-4 probe performs separately parameterized real SQLite/CAS agent and user mutations, crashes after adapter commit but before supervisor receipt, reconciles once, then restores a previous bundle with an unreconcilable model dispatch still reserved and suspended.
- Guarantee-2 forged-facts probe rejects a candidate Measurement append and scores the unsolved committed state as zero despite a success claim.
- Existing core + acceptance checks: 108 passed, 2 xfailed in 104.67s. Guarantee 4 is now passing; guarantee 2 retains the protected-feedback xfail and guarantee 1 retains native-jail xfail.
- Saved second-benchmark freeze hashes before adding the independent integer-target adapter. The acceptance test checks the same hashes before and after discovery/init/mutation/crash/reconcile/scoring/offline replay.
- Fresh replay passes with no adapter installation and with the light adapter distribution installed into an isolated directory and import-blocked. uv's offline wheel installer also panicked, so the test installs our purelib-only wheel with a small RECORD-backed file installer. This proves the adapter import boundary; it does not claim tau2 or its transitive closure was installed.
- Extended state-reference support in Measurement before the second-benchmark freeze so opaque intermediate snapshots are directly covered by pure replay integrity checks.
- Added a user-batch recovery test with authenticated model capture and a crash between two tool calls. Exactly one model dispatch and one user-turn commit remain after restart.
- Tightened telecom construction to verify task/reward bytes against the qualified source inventory and bind operation storage to a retained adapter implementation identity.
- Native snapshot reopening suppresses constructor synchronization, preserving mutations from tools that failed before upstream synchronization. Subsequent tool execution still uses upstream get_response unchanged.
- Upstream source checks confirmed Tool.openai_schema and the actual UserSimulator interception seam. The implementation calls upstream action and deterministic evaluator code rather than copying it.
- Added explicit materialized-data byte verification and a certification CLI; the caller must provide a reviewed assertion allowlist. No inventory certificate is inferred from data presence.
- Added a bounded dependency-light RPC client and closed server dispatch in the adapter package. The root composition need not import the telecom implementation, operation store or scorer. A real subprocess test with the synthetic backend passed discovery, operations and scoring while blocking live tau2/heavy imports.
- The common-core freeze remains unchanged after the RPC work. All subsequent implementation edits stayed in the adapter package and tests.
- Qualification now retains the assertion allowlist and normalizes unordered goal components while preserving DB reference-action order.
- Final focused M5 run: 59 passed, 3 explicitly skipped in 26.64s. Strict mypy passes across 134 files. The final adapter-only initialization guard was checked by 5 passing RPC/scoring tests. Full repository testing is still running; only the two expected packaging-test failures have been reported so far, pending final traceback confirmation.
- Full-suite integration found M4's `test_core_unchanged_and_adapters_selected_by_manifest` still requiring pre-M5 hashes for broker.py, supervisor.py and verify/engine.py. Updated only those three explicitly approved paths to the retained M5 freeze and added an assertion that they are the exact delta. All other historical M4 core hashes and manifest-selection checks remain intact. The old baseline file was not rewritten.
- Full repository run completed: 655 passed, 9 skipped, 3 xfailed, 4 failed in 780.35s. Two failures are exactly the known uv build macOS system-configuration panic. The other two were the M4 historical hash assertion and `test_broken_candidate_suspends_and_operator_can_restore`, which expected CONTINUE after restoration. Updated the latter to SUSPENDED and checked unchanged effects/environment/private state. Both corrected assertions pass together. No production core change was needed.
- Final strict mypy remains clean across 134 files. Final shared-core hashes still match the pre-second-benchmark freeze. Existing-code changes are retained as `existing-code.diff`; new code lives at the report-listed source paths. No commit or PR was created.
