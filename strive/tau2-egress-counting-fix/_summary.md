Counting and generation now share one OpenAI egress worker, captured and readied by the [live campaign](../src/strive/vnext/cli/campaign.py) before jail qualification. The [transport](../src/strive/vnext/harness/openai_live.py) checks worker identity and namespace before each HTTP connection, and diagnostics distinguish worker routing from proxy selection. Deterministic tests cover transports created after jail entry, counting/generation namespace equality, readiness, and retained reservations after failure. Strict mypy passes, the full host suite passes 839 tests, and all 30 frozen core hashes remain unchanged; no container, live API call, or commit was made.
