Strive vNext now has an isolated artifact root, durably published SHA-256 objects, framed authenticated journals, and leased writer epochs. The [pure verifier](../src/strive/vnext/verify/engine.py) reconstructs effects, results, revisions, continuations, and accounting while treating annotations as opaque bytes. Runtime guarantee 5 now passes in a guarded fresh interpreter, and the [acceptance report](README.md) maps every required attack to its test. Strict mypy and 167 vNext tests pass, with four later-milestone xfails; the remaining full-suite blocker is uv crashing before the two legacy wheel builds on this macOS sandbox.
